# Core dependencies
torch>=2.1.0,<3.0.0
# Upper bound was <5.0.0, which EXCLUDED what production serves: the
# nexusmind-scorer venv on gpu-server runs transformers 5.0.0 (verified
# 2026-08-09 off the live service's own interpreter,
# /home/hcl/gpu-server/nexusmind-scorer/venv). A declared range that the
# deployed environment violates cannot be used to reproduce production.
transformers>=4.35.0,<6.0.0
datasets>=2.14.0,<3.0.0
anthropic>=0.8.0,<1.0.0
# BOTH Gemini SDKs are in use and both must be declared.
#   google-generativeai (old): filters/common/commerce_prefilter/v1/oracle.py,
#       ground_truth/llm_evaluators.py, scripts/test_gemini_models.py
#   google-genai (new):        ground_truth/batch_scorer.py:569,579
#       -- `from google import genai`, the path --llm gemini-flash takes.
# Only the old one was declared, so a clean install could not run the Gemini
# oracle at all (found 2026-08-09 when the investment_risk oracle check failed
# with ModuleNotFoundError: No module named 'google').
google-generativeai>=0.3.0,<1.0.0
google-genai>=1.0.0,<2.0.0
openai>=1.6.0,<2.0.0
pydantic>=2.5.0,<3.0.0
tqdm>=4.66.0,<5.0.0
pandas>=2.1.0,<3.0.0
numpy>=1.26.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0
wandb>=0.16.0,<1.0.0
pyyaml>=6.0.1,<7.0.0
jsonlines>=4.0.0,<5.0.0
fastapi>=0.104.0,<1.0.0
uvicorn>=0.24.0,<1.0.0
python-multipart>=0.0.6,<1.0.0
safetensors>=0.4.0,<1.0.0
bitsandbytes>=0.41.0,<1.0.0  # For 8-bit model quantization
peft>=0.6.0,<1.0.0

# Development dependencies
pytest>=7.4.0,<9.0.0
pytest-cov>=4.1.0,<6.0.0
black>=23.11.0,<25.0.0
ruff>=0.1.6,<1.0.0
mypy>=1.7.0,<2.0.0
ipython>=8.17.0,<9.0.0
jupyter>=1.0.0,<2.0.0
