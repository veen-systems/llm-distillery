# Memory Index

<!-- verify: python3 scripts/verification/check_index_budget.py -->
<!-- verify: python3 scripts/verification/check_index_budget.py --target project -->
<!-- verify: python3 scripts/verification/check_index_budget.py --target loaded -->
<!-- verify: python3 scripts/verification/check_index_budget.py --target pointers -->
<!-- verify: python3 scripts/verification/check_doc_claims.py -->
<!-- verify: python3 scripts/verification/check_claim_shapes.py -->
<!-- verify: python3 scripts/verification/check_training_provenance.py -->

⚠️ **This index is budgeted in SIZE, not lines** (bytes — the guard reads `"rb"`; say bytes when quoting it). It reached **54,808 chars in 91 lines** on 2026-08-15 (up 69% in three days) because entries grow by lengthening, so a line-count heuristic never fires. Session detail belongs in `project_session_*.md`; an entry here is a hook that answers *should I open that file?* ⛔ **This file is NOT auto-loaded** — it is reached by a `CLAUDE.md` pointer row. The always-loaded layer is `CLAUDE.md` plus the USER auto-memory index, budgeted together by `--target loaded` (#138).

⛔ **THE SESSION LOG HOLDS FOUR ENTRIES. The fifth is MOVED to [`session-log.md`](session-log.md), never compressed.** (#123, 2026-08-25.) The index was 69% session entries by character and grew ~600–900 per session against a fixed ceiling; hand-trimming recovered ~100 chars per line removed, so it lost roughly 6:1 — and every trim turned a finding into a pointer, which is how the always-loaded layer quietly stopped being a record. **Moving costs nothing and loses nothing.** A revision to an existing entry is as expensive as a new one, so budget for both.

*Standing rules moved to `CLAUDE.md` § Hard Constraints → "Working rules" on 2026-08-06 (context audit): they are always-needed constraints, and this file is navigational. This index is pointers + session log only.*


- **2026-09-10 (evening) — a vendor swapped our oracle under us, a detector shipped, and four review lenses found five blockers in it** ([session](project_session_2026_09_10_evening.md)) — **< $0.01**, six DeepSeek probe calls, no oracle scoring. **EXP-037**; `H-HD1`–`H-HD4`; NexusMind **PR #474 is a DRAFT**. ⛔ **Nothing deployed touched, no filter package changed, no merge.** ⭐⭐ **THE KEEPER — I PUBLISHED A NULL-ARM READING WHERE THE NULL COULD NOT HAVE SAID YES.** The `EXP-037` sweep concluded *"and the null is still 0.0"*; the null's **flag count**, which that table omitted, is **0.0 of 137 at thresholds ≥0.65 in all five seeds**, so its catch of zero is **arithmetically forced**. At 0.30 the null fires **MORE** than the real arm (22.4 vs 10.6), so *"it fires at twice the rate"* holds only at the pre-registered rule. **RETRACTED**; the PRIMARY (2.0 of 9 vs a null of 0.0 at twice the rate) is unaffected. **The instrument rule is in `CLAUDE.md` and I broke it in the document where I was congratulating myself on having a control.** ⛔ **Two numbers wrong, both flattering the artifact:** *"single seeds bounced 0–3"* over the plateau — they span **2–4**, and at 0.30 **three seeds beat the ensemble**; and *"three of five seeds under 2%"* was **two**. ⭐ **DeepSeek renamed the flash model and our guard silently stopped covering it** — `startswith("deepseek-v4")` is a **denylist the vendor can extend**, and `deepseek-flash` is now the only flash id advertised; two entry points had no guard at all. ⛔ **`#157`'s BOTH proposed fixes are DEAD: the served version is NOT OBSERVABLE** — response `model` reads `deepseek-flash` whatever you send, no header carries a version, and there is **no versioned id to pin**. ⚠️ The reasoning trap's symptom CHANGED and no longer raises: at production `max_tokens` the literal id returns **correct JSON** and bills **208 tokens against 6** (n=1). ⛔ **Review refuted my own scoping — SIX DeepSeek call sites, not three**, and the host check falls to a root-anchored FQDN. ⭐ **EXP-037**: primary **2.0 of 9** vs null **0.0**, decision rule's 2–4 bucket → *stamp anyway, the $3.2–3.6 pool becomes a ranked option.* Shipped `harm_detector/v1`, 5-seed ensemble, **no threshold**; CPU↔CUDA **0 verdict flips** (⛔ do not generalise — the student's term is **0.1956**). ⛔ **PR #474's five reproduced blockers:** `enabled: false` is NexusMind-side only and an unwrapped lifespan load takes the **whole scorer** down; a **twin stamp list** left un-updated turns every stamped row into a Contract A violation **blamed on FluxusSource**; `r.get("score")` makes a server shape change a **permanent silent zero-stamp no-op** (250 rows, 0 stamped, clean exit); a hardcoded tuple means a crash **pings OK**; and the rewrite **deletes unparseable lines** from `data/raw` invisibly, **between obituary's stamp and its enforcement**. ⛔ **Two process failures: I ran `git stash -- .`** (whole-tree verb, prohibited) and **dismissed collection errors as "the environment"** for the second time — it was `python3` vs `venv/bin/python`; 1,643 pass. ⭐ **ALL OF IT FIXED THE SAME SESSION.** Owner: NexusMind consolidates to ONE GPU-HOSTED PACKAGE soon, so the gpu-server endpoint was **scaffolding for a boundary that goes away** — `NM#474` rewritten in-process (**-8,660 / -3,894 chars**), and **blockers 1 and 3 were DELETED, not patched**. Measured **19.9 articles/s on CPU** (~3 min/cycle on the 8-core host); cold start bounded to 3,000/run, **newest first** (unbounded = 34,661). ⛔⛔ **THREE OF MY OWN FIXES REPEATED THE SHAPE THEY FIXED — cheap checks placed AFTER expensive work**: the scaler unpickled BEFORE its hash check, the `sentence_transformers` import ABOVE every integrity check (unreachable in CI), the empty-ensemble guard AFTER the embedding. **Verify before you load; refuse before you spend.** ⛔ **My DeepSeek surface count was wrong TWICE** — three, six, then **eight** from a test that greps the tree; ⭐ **enumerate a surface from the CODE, never a list.** ⛔ **One mutation SURVIVED** — deleting the per-article cap passed all 11 tests because the only cap test exercised FILE-level deferral. **11 + 6 mutations killed; 790 + 1,649 pass.** `NM#474` is a **DRAFT** — owner deferred the review round and smoke test. ▶ **NEXT: review + smoke-test #474, then the ~$0.92 parity run (arm A FREE — 6,130 rows carry k=3 V4 votes), then three open decisions.**

- **2026-09-10 — the frozen-mpnet detectors: we already run a BERT, and two corrections against myself** ([session](project_session_2026_09_10.md)) — **$0**, no oracle calls; `8846369`, `e68838a`, `de3066d`. **EXP-033**–**EXP-036**, **H-DET1**–**H-DET6**, issues **#158**/**#159** opened. Window 128→512 **REJECTED** (bands overlap; violence LOSES recall; 3.82× GPU). v3/v4/v5 ordering **UNUSABLE** — three orderings across five seeds, v4's mean the HIGHEST, reversing ovr's ADR-042 flag. ⭐ **44% of misses are a title/body POOLING conflict**, not euphemism — and I had called that mechanism refuted after testing a peer's two EXAMPLES rather than the CLAIM, then called dilution "bounded" against **zero** when the bar is the **op-point**. ⛔ Detector blindness to non-Latin **REFUTED** (Latin 1.252% vs non-Latin 1.249%, 319,156 production rows) — the non-Latin corpus build is **deprioritised**. ⛔ `v4_train_seed.jsonl` is NOT v4's corpus; `v4b_train_seed.jsonl` is. ⭐ **Every shipped detector metric is ONE SEED** (recall 0.6599–0.8081, #158). Nothing deployed touched.

- **2026-09-09 — the corpus that cannot exhibit its own failure, four catches in a peer's pre-registration, and a review that found 5 blockers in my write-up of both** ([session](project_session_2026_09_09.md)) — **$0**, no oracle calls, no production code changed. **EXP-032**; `H-AP1`–`H-AP6`; one Dead End; six gotchas. ⛔ **NexusMind untouched, nothing deployed, no merge.** ⭐⭐ **THE KEEPER — 1,253 harm rows and none of the one that matters.** `labels_v84_merged.jsonl` holds **1,253 `harm_is_subject`** (**19.03% sample / 20.96% design-weighted**), which reads as *"not short of harm data"* and is backwards: **max `weighted_mean_all` 2.7667**, **0** at ≥4.0, and **all 316 rows at or above 4.50 are `in_scope`** (4.798% / **2.709%**). ⇒ **the corpus's FINAL LABELS cannot exhibit STUDENT/oracle disagreement, because they ARE the oracle's output.** ⭐ **Mirror of a rule already in `CLAUDE.md`: *prove the instrument could have said yes* guards a NEGATIVE — a POSITIVE COUNT of 1,253 carried as little, and a big reassuring number is the harder half because nobody interrogates it.** Cross-tab the verdict against the SCORE BAND, not the verdict. ⛔⛔ **AND MY OWN ABSOLUTE WAS REFUTED BY THE REVIEW: *"zero examples and cannot have any"* / *"the only route"* are FALSE** — **178** rows carry ≥1 harm run-vote with a non-harm final verdict (**1** above the op-point), **1,079** `scope_flipped`, **178** harm rows with split run votes, and a **$0** detector arm was never costed. `analyze.py` never read `scope_verdicts_per_run`, a key in the same JSON object. ⛔ **Three defects in the analysis itself, all found by review, none by the checkers:** (1) a `weighted_mean_major`→`weighted_mean_all` **fallback unioned two labelling generations** and published **351/5.3%** for the declared **316/4.798%** — the corpus states `aggregate_used == "all"` on 6,586/6,586 and **the shape guard passed while the semantics failed**; (2) the headline was a **hardcoded `print`** — two lenses mutated the DATA and it printed *"ALL 351 … cannot have any"* verbatim, exit 0 (now 6 assertions, all 6 mutations killed); (3) **every share was unweighted on a 25.1× stratified draw** and `check_claim_shapes.py` PASSED because the file was a **sixth path alias** absent from `DESIGN_WEIGHTED` — added, which exposed 5 more previously-invisible reads. ⛔ **The above-op population is ONE PROMPT ARM**: 316/316 from a 456-row `prompt-v8-4.md` relabel set (69.30%) while the 6,130-row arm has **0**, and `H-V8-30` measured that swap moving `in_scope` **+0.1774** (p=0.0034). ⭐ **κ 0.375 is the wrong statistic AND the κ rescue fails** — binary-`harmful` κ is **0.3749**, indistinguishable; the screen rests only on **NESTING** (DeepSeek **9** ⊂ Gemini **32**, 0 reversals, nested **within every stratum**: 2⊂14, 3⊂8, 4⊂10). ⚠️ **P=7.83e-07 tests independence between two judges reading the same articles — guaranteed false, so correlation, not screen safety**; safety is 0 misses in **9**, rule-of-three bound **33%**. ⚠️ **The fluency finding is SUBSTANTIALLY COMPOSITION**: pooled `fits` −15.1/−12.3pp en→non-en, but **the sign REVERSES within v8_only** (+12.4/+13.7) under both judges; `H-AP1` OPEN, method must stratify. ⛔ **DeepSeek's k=3 split-vote rate is 43.1%, not 45.8%** — 45.8% is the non-English rate, and I cited it as the instrument's *inside the gotcha about borrowing a floor across populations*. ⭐⭐ **The peer's pre-registration bought four catches, sharpest: A CONTROL WHOSE FAILURE MODE IS SILENCE** — `judge.py`'s cache keys `(id, pass)` on `<out.json>.partial.jsonl`, so an arm reusing the main path returns cached verdicts with **no API call**, flips at **0%**, and the rule read that as *"sound and earned"*. **The reassuring answer is the one the bug produces.** ⚠️ Its fix needed a fix: a **file-total** line count still passes for an interrupted-then-rerun arm — count lines appended by THIS process. ⛔ **Two of my own suggestions were wrong** (a replacement void condition assuming what the blind design prevents — declined, slot left EMPTY; and **a floor borrowed across the JUDGE and its SAMPLING STRUCTURE**, where at k=1 the quantity does not exist). ⛔ **Lexical adverse-example selection is DEAD for the thriving family** (`H-AP6`, ⚠️ scoped — keyword corpus work elsewhere unaffected; peer-reported, **not adjudicated**). ⚠️ **EXP-031 cannot be replayed FROM THIS DIRECTORY** — `panel_frame.jsonl` has **no `content`** on any of 137 rows; ⚠️ *not* unrecoverable, the 730-day sadalsuud archive was never checked. ⭐ **Persist the exact model INPUT, not the outputs plus a frame you believe reconstructs it.** ⏸️ **~$3.2–3.6 + ~$0.07 AWAITS THE OWNER, and is no longer the only option** — cost the two $0 routes first. ▶ **NEXT: `#156`, TODO Step 0b — Step 2's framing is amended, and the free arms come before the spend.**

- **2026-09-08 (third) → 09-09 — the field produced at label time and destroyed at split time, and the review mutated my DATA** ([session](project_session_2026_09_08_third.md)) — **$0**, no oracle calls, `dc91cbd`. **#155 CLOSED**; **#156 and #150 unblocked and commented.** ⛔ **Lane C's only blocker is gone; lanes A and B untouched** (owner: *"keep the lanes open"*). `training/prepare_data.py` enumerated a six-key allowlist, so everything the oracle emitted past the six dimension scores died at split time — silently, `COMPLETE`, exit 0. Now every source field passes through and the analysis block rides whole under a stable **`oracle_meta`**: `scope_verdict` on **6,586/6,586** split rows, `harm_is_subject` **1,011 / 105 / 137**, **$0** of new oracle spend. ⭐⭐ **THE KEEPER — the review mutated the DATA, not the code, and my tests structurally could not.** Keeping only non-list values in `oracle_meta` passed **all 35 tests** while the real corpus lost `scope_verdicts_per_run` on **6,586/6,586** rows: my fixture said *"shaped like `labels_v84_merged.jsonl`"* and held **12 scalar keys against the real 20**, omitting every list and dict. *Enumerated, not allowlisted* was true **of the wrong population** — #155's own failure mode inside the thing built to prevent it. **A fixture IS a hand-built population**; fifth mutation rung. ⭐ **The fix was also incomplete**: `scripts/merge_training_data.py` writes the same three files with the same defect and had **no tests** — and passes old rows through whole, so a merge is *partially* covered and a `scope_verdict` rate read off it is a coverage artefact. Fixed, plus a coverage warning; `validate_training_data.py` now checks the **artifact**, which nothing did, which is how this survived a training cycle. 3 blockers / 4 warnings from three lenses; **10 mutations, all killed; 779 passed**. ⚠️ **`oracle_meta` is VERBATIM and heterogeneous** — `runs` is a **list** on 6,130 rows and an **int** on 456, `weighted_mean_major` absent on those 456, dimensions `float` vs `{"score":…}`; **`scope_verdict` is the only key on all 6,586.** ⭐ **`b11e42f`'s split-redraw caution RETIRED for this input**: seed 42 reproduces 5,268/658/660 with identical order and **0 rows moved** — ⛔ **but I committed that negative BEFORE its control.** Seed 43 moves **1,044/584/599**, tiers +0.5 moves **1,041/593/580** (sizes 660→661), so the instrument can say yes and the zero is real; **a CONFIRMING control supplies no pressure to run it**, 23rd occurrence. Bytes still change (`e361b517…`→`e524c632…`), annotated on the pinned gate manifest. ⛔ **NexusMind untouched, nothing to deploy.** ▶ **NEXT: #156** — pre-register, rebuild splits to a NEW dir, train the binary detector, **STAMP ONLY**, judge on specificity.

- 📓 **Older sessions — [`memory/session-log.md`](session-log.md)** — every entry before the four above, VERBATIM and unabridged (**55** as of 2026-09-06 — `grep -cE '^- ' memory/session-log.md`, do not quote this from memory). The index keeps the newest four; `/curate` MOVES the fifth into the log rather than compressing it, so no finding is ever shortened to buy space. **Look there before concluding something was never recorded.**

- [Date errors & the recency boost](date-error-recency-boost-hypotheses.md) — a flat recency boost on `published_date` means **any** date error landing a row inside the window wins it, invisibly. Both hypotheses RESOLVED. ⚠️ **`collected_date − published_date` is NOT a fabrication instrument without a `source` breakdown** — a fixed-publication-moment source impersonates the signature once per daily cycle. Read before touching dates, the boost (`NexusMind/src/scoring/display_ranking.py`), or any age-derived figure.
- [Hypothesis ledger](hypothesis-ledger.md) — ⭐ **START HERE to recall prior work.** Every hypothesis, its verdict, and where its experiment lives, across 10 files, two repos. ⛔ **`H4` is defined in FOUR files; three files have no ids** — always qualify. Corroboration hypotheses live as `INST/OBS/ART/PROP` rows in NexusMind's V&V registry, the only place recording instrument **certification**.
- [Corroboration feature hypotheses](corroboration-feature-hypotheses.md) — the cosine/time/NER feature set **already exists and was already measured**; what is confirmed, refuted and untested, with the values. **The threshold is not the lever** — the biggest one is a gate, not a feature. Shared-number features refuted; time confirmed and still switched off. Read before proposing any NER or matching-feature work.
- [Google News corpus hypotheses](google-news-corpus-hypotheses.md) — the largest single population in the corpus, and **every number there carries its denominator because four denominator errors were made deriving it**. Five claims already REFUTED. ⚠️ **Never oracle-re-score a GN row**, never match GN on a `gn_` key prefix, and always name whether you mean feeds, rows or items — they differ by up to 5×. Read before quoting **any** GN number.
- [CD v6 probe hypotheses](cd-v6-probe-hypotheses.md) — #98: what the e5 probe confirmed (per-language gap gone; batch-invariant, unlike #95), what it refuted (screening is a REGRESSION; 4 of 5 "recovered" positives are off-lens), and the traps (v6 cannot score — no inference module, no calibration)
- [Cross-repo prioritization](cross-repo-prioritization.md) — master issue landscape across 7 repos. ⛔ **No board count is stated here or at the top of that file — run the `gh` loop at the head of it and quote the result with a timestamp** (removed 2026-08-17, deliberately; do not restore). **The count is never the finding, the distribution is**: a small minority of open issues is touched in any two-day window, ovr.news is the largest board and the least active, and two boards are frozen — all of it re-derivable from that one query. Also holds the four live chains, with the FS#145-is-an-enabler and NM#314-is-built corrections.
- [Stamp & contract integrity](stamp-contract-integrity.md) — **the stamps are a per-article feature set (~40+ numbers), and nothing validated them.** The schemas check SHAPE only and are permissive. **Read before adding a stamp or trusting a stamped field.** ⚠️ **The contract is being REDESIGNED, not patched — read `docs/proposals/contract-a-redesign.md` first; #112 supersedes #111.** **The rule: a field belongs in Contract A iff only the collector can know it AND it is destroyed if not recorded now.** ⭐ **Greenness is not evidence a schema tracks reality.** Also holds the five instrument traps (the validator counts **errors, not rows**), the seven exclusive+perishable categories (**G, the non-event, is the one no symptom surfaces**), and two **superseded** counts kept to date their correction — never re-quote either.
- [Opinion/editorial genre hypotheses](opinion-genre-hypotheses.md) — #121 over 233,338 rows: the within-source control **dissolves the effect in 5 of 6 lenses**; only `investment_risk` survives, and a topic confound survives inside each source. The numbers and the op-point trap are in the `CLAUDE.md` pointer row; the derivations are here. Read before quoting any #121 number or building a genre stamp.
- [Filter status](filter-status.md) — current state of all production filters and in-development versions
- [NexusMind data sources](nexusmind-data-sources.md) — what each production artefact EXCLUDES; `filtered_*.jsonl` also drops source-type-excluded rows, `data/raw/` is pre-enrichment
- [Prefilter & length-floor hypotheses](prefilter-length-floor-hypotheses.md) — what each prefilter actually blocks (measured 2026-08-02); #93 shipped + synced 2026-08-03; read before any enforcement flip
- [Batch-shape score noise](score-batch-shape-noise.md) — **a score is not a function of the article alone**: batch composition alone moves it and flips near-boundary surfacing decisions (#95). ⚠️ **A floor belongs to a population and a mechanism, not to a project** — **five measured terms, and not all are floors**: batch **0.16**, library stack **0.2008**, device CPU→CUDA **0.1956**, oracle decoder **0.436/0.687**, and **host 0.0000** (⛔ **not "cross-box"** — the machine contributes nothing once pins and device match). **Picking one by magnitude looks like rigour and is not a method.** ⛔ **One more is not a floor at all**: `human_thriving v8`'s scope gate is a **step function**, so `1/√k` does not touch it — **5.3% production-mix / 6.7–9.3% at the boundary (n=200)**, and *not distinguishable* from the earlier 13% panel figure. Re-measure per population; never inherit a floor. Read before any threshold or op-point measurement.
- [Enrichment-delta hypotheses](enrichment-delta-hypotheses.md) — **NM#310 is a compute story, not a quality story** (median +0.112, 3.6% of gate crossings, all upward). **H-E1 RESOLVED** — `nature_recovery`'s +0.023 is a **CANCELLATION**, not inertness; the finding that travels is that **enrichment's dominant effect is on evidence-quality-type dimensions** (`cultural_discovery`'s `evidence_quality` **+1.433**, 46/47 rows positive) and `nature_recovery v4` has no evidence dimension. Three open entries: H-E2 (shortest stubs gain LEAST), H-E3 (DeepSeek vs Gemini differ in **slope not offset**), H-E4 (`discovery_novelty`).
- [GPU server](gpu-server.md) — venv, PYTHONPATH, HF_HUB_OFFLINE, ollama conflict, training setup
- [b650 GPU](b650-gpu.md) — 3090 Ti node. **`venv-prodparity` is parity with GPU-SERVER (not sadalsuud); on CPU b650 is bit-identical to it, 660/660.** ⛔ **Production serves on GPU**, so match the DEVICE before diffing any replay against stored production scores — CPU→CUDA is 0.1956, 3 flips at the deployed 4.5. Access, two venvs, staged data
- [Gotcha log](gotcha-log.md) — problem/fix archive
- [Calibration history](calibration-history.md) — Dead Ends section: approaches known to fail (#69); read before calibration/scorer/oracle-prompt work
- [Gemma-3 model](gemma3-model.md) — Auto mapping fix, key format details; read before debugging model loading or PEFT issues
- [Oracle pricing & scheduling](oracle-pricing-scheduling.md) — ⛔ **Gemini Batch is a price we CANNOT PAY** (no `.batches` call site, 2026-08-25): among IMPLEMENTED paths DeepSeek off-peak wins by 1.74×. The answer is a RATIO (I/O < 8.4), and the real lever is the per-prompt cache ceiling, 1.5%–35.7% (#131). Weekends bill off-peak
- [ovr lens set](ovr-lens-set-current.md) — current lens→filter→tab mapping
- [Filter doc standard](filter-doc-standard.md) — deployed filter documentation set
- [CD v5 reference status](cd-v5-reference-status.md) — cultural_discovery v5 as DeepSeek-oracle reference example, ADR-020 methodology
- [Obituary detector](project-obituary-detector.md) — enforcement state, the live false negatives, carryover, and the four SSH verify assertions. Read before touching the junk gates or reading their state. ⭐ **2026-09-10: 44% of its misses are a title/body POOLING conflict** (#159), and it is NOT blind to non-Latin scripts (1.252% vs 1.249%)
- [Obituary v4 hypotheses](obituary-v4-hypotheses.md) — small-N hard negatives work, panel beats oracle labels; "FNs are cheap" FALSIFIED 2026-07-30. ⛔ **Its v3/v4/v5 numbers are SINGLE-SEED** — 2026-09-10 addendum; the ordering does not survive (#158)
- [Violence promotion v1 hypotheses](violence-promotion-v1-hypotheses.md) — recipe transfer confirmed, prompt boundary holds, recall gap is the key open question
- [Uplifting v7 training](uplifting-v7-training.md) — training history; v7 deployed (hybrid inference)
- [Thriving v1 scoring](thriving-v1-scoring.md) — PARKED indefinitely (ADR-015); scoring status, resume commands
- **Before 2026-08-01 — 23 session files, no longer indexed line by line** (trimmed 2026-08-25 for the character budget, #123; the files themselves are untouched): `ls memory/project_session_2026_0[57]*.md`. They carry the deployment history of cd v5, nr v4 and solutions v4–v6, the 2026-07-17 deploy hardening, and the 2026-07-31 refit day. ⛔ A pointer is not a summary — open the file, do not infer the session from its name.
