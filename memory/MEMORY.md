# Memory Index

*Standing rules moved to `CLAUDE.md` § Hard Constraints → "Working rules" on 2026-08-06 (context audit): they are always-needed constraints, and this file is navigational. This index is pointers + session log only.*

- [Corroboration feature hypotheses](corroboration-feature-hypotheses.md) — **the cosine/time/NER feature set already exists and was already measured** (NM#188/NM#301; NM#213 is closed): it failed at **n=23 with one case supplying 21 positives**, and `title_cosine` failed identically — so this is not "NER doesn't work". **Updated 2026-08-09: the panels WERE adjudicated (the "UNADJUDICATED" claim was wrong — verdicts are in a gitignored NexusMind dir never copied to b650); `title_body@0.96/0.92` is REFUTED and the turn-over point is `0.94/0.90`, which still only TIES live under #95; the biggest lever is a GATE not a feature — primary literature is 20–30% of all merges and 0–14% correct; shared-number features REFUTED (AUC 0.503–0.581); time CONFIRMED at 0.809 and still switched off.** **Updated 2026-08-09 (later): INST-10 is now CERTIFIED but PRECISION-SIDE ONLY — it CANNOT authorise `temporal.enabled: true`, because PROP-1's falsification is a *recall* test that has never been run, and the shipped `sigma_hours: 72.0` is the REFUTED value (two-sided window is σ ∈ [6,36]). Two claims in that file are now REFUTED: FS#143's "removes 100% of the class / 0 titles in ≥2 category feeds" (the category feeds duplicate EACH OTHER 738×/7 days, and 123 titles were lost with the dropped feed), and "Contract A is `additionalProperties: false`" (`metadata` is open in both contracts; the detector shipped at COLLECTION).** **Read before proposing any NER or matching-feature work**
- [CD v6 probe hypotheses](cd-v6-probe-hypotheses.md) — #98: what the e5 probe confirmed (per-language gap gone; batch-invariant, unlike #95), what it refuted (screening is a REGRESSION; 4 of 5 "recovered" positives are off-lens), and the traps (v6 cannot score — no inference module, no calibration)
- [Cross-repo prioritization](cross-repo-prioritization.md) — master issue landscape across 5 repos. **Re-queried 2026-08-09 (later): 195 open** — LD 35 · NM 43 · ovr 89 · FS 13 · ps 12 · atlas 3. *(NM#296 closed by PR #299; FluxusSource down 16 → 13 as its own session closed FS#143 and others.)* Prior line, kept for the trend: **Re-queried 2026-08-07 (NIGHT): 198 open** (LD 36 · NM 42 · ovr 89 · FS 16 · ps 12 · pipeline-atlas 3), **~81 untouched 30+ days**. *(Earlier lines in that file still show 195/196/197 from successive same-day passes; 198 is the verified current figure — the late pass never re-counted after filing its own FS#133/#134.)* Read **Ordering 2026-08-07** then **Chain verification 2026-08-07 (late)**, which re-queried every chain link and found **two marked ✅ while open** — LD#73 is bookkeeping (shipped as `violence_promotion v1`), **NM#185 is not**: Chain 1 is *not* complete, it carries an unstarted **commerce prefilter v3** whose evidence base was 100% `sustainability_technology`, a filter deleted 08-03. Also: **persuasion-scorer's 12 issues were counted in every total and banded nowhere** → now Chain 16. The P0-P4 tables below those sections are the 08-05 state, repaired twice on 08-07 but historical by design. **All seven owner decisions are taken.** Chain 10 re-rooted on NM#301; Chain 11 CLOSED; Chain 3 CLOSED; Chain 4 re-rooted on #93; Chain 8 deadline-driven (FS#120 ~2026-08-14). Two standing traps recorded there: the decision list drifts faster than the issue list, and a findings list is a sample, not an inventory.
- [Stamp & contract integrity](stamp-contract-integrity.md) — **the stamps are a per-article feature set (~40+ numbers), and nothing validated them.** Contract B checks SHAPE only, is permissive, and had never met a production row until 2026-08-08 (first run: 908 violations — `image_confidence` declared 0–1 is a raw logit). `scripts/stamp_census.py` covers population + consumers. **Read before adding a stamp or trusting a stamped field**
- [Filter status](filter-status.md) — current state of all production filters and in-development versions
- [NexusMind data sources](nexusmind-data-sources.md) — what each production artefact EXCLUDES; `filtered_*.jsonl` also drops source-type-excluded rows, `data/raw/` is pre-enrichment
- [Prefilter & length-floor hypotheses](prefilter-length-floor-hypotheses.md) — what each prefilter actually blocks (measured 2026-08-02); #93 shipped + synced 2026-08-03; read before any enforcement flip
- [Batch-shape score noise](score-batch-shape-noise.md) — **a score is not a function of the article alone**: batch composition moves it up to 0.17 and flips 7-9% of near-boundary surfacing decisions (#95). Read before any threshold/op-point measurement
- [GPU server](gpu-server.md) — venv, PYTHONPATH, HF_HUB_OFFLINE, ollama conflict, training setup
- [b650 GPU](b650-gpu.md) — Arian's 3090 Ti training node: access (account jeroen), uv venv, staged data, cross-box score-skew warning
- [Gotcha log](gotcha-log.md) — problem/fix archive
- [Calibration history](calibration-history.md) — Dead Ends section: approaches known to fail (#69); read before calibration/scorer/oracle-prompt work
- [Gemma-3 model](gemma3-model.md) — Auto mapping fix, key format details; read before debugging model loading or PEFT issues
- [Oracle pricing & scheduling](oracle-pricing-scheduling.md) — DeepSeek V4 peak/valley pricing; schedule batch jobs off-peak
- [ovr lens set](ovr-lens-set-current.md) — current lens→filter→tab mapping
- [Filter doc standard](filter-doc-standard.md) — deployed filter documentation set
- [CD v5 reference status](cd-v5-reference-status.md) — cultural_discovery v5 as DeepSeek-oracle reference example, ADR-020 methodology
- [Obituary detector](project-obituary-detector.md) — ENFORCEMENT ON v5@0.85, VERIFIED + overnight-checked 2026-07-31; site carryover washes out ~Aug 13 (owner: no purge); 2 live v5 FNs banked on LD#85 (parked)
- [Obituary v4 hypotheses](obituary-v4-hypotheses.md) — small-N hard negatives work, panel beats oracle labels; "FNs are cheap" FALSIFIED 2026-07-30
- [Violence promotion v1 hypotheses](violence-promotion-v1-hypotheses.md) — recipe transfer confirmed, prompt boundary holds, recall gap is the key open question
- [Uplifting v7 training](uplifting-v7-training.md) — training history; v7 deployed (hybrid inference)
- [Thriving v1 scoring](thriving-v1-scoring.md) — PARKED indefinitely (ADR-015); scoring status, resume commands

## Recent Sessions

- [2026-08-09 evening](project_session_2026_08_09_evening.md) — **sent to fix a calibration defect; there wasn't one, and two of my own headlines died the same day.** The 173× surfacing spread is mostly **genuine base-rate difference**: 160 production articles oracle-graded put `nature_recovery` at **87% precision** with ~8 true positives per 2,190 articles, and the bands below student-1.0 — **96.7% of the corpus** — returned **0 of 50** with an oracle *max* of 1.05. **RETRACTED #1**: the isotonic crush is real and measured (`recovery_evidence` jumps **+2.47 over a 0.06 student step**, >52% of the range maps to ≤1.33 against a 3.75 gate) and worth **~3 articles/cycle** — a mechanism can be real and not be the lever. **RETRACTED #2**: a six-filter MAE ranking and "calibration is a net negative", both artifacts of the metric. Owner: *"beware of what you call error! we are doing needle-in-haystack filtering"* → **ADR-023**, promoted to CLAUDE.md Hard Constraints: **a false positive costs a reader, a false negative costs nothing visible** → optimise specificity, recall is a floor, **never rank on MAE**, and only recall+specificity are comparable across splits (precision is base-rate dependent too). Careful — composition explains only **27%** of the uplifting↔nature_recovery MAE gap, so the residual may be real. **ONE IRREVERSIBLE RISK CLOSED**: `uplifting v7`'s adapter existed on **one disk** (NO_HUB + gitignored) — now on four machines md5-verified; **three production probe pickles existed only on gpu-server** and a sync would have deleted them (cause: the `.gitignore` negation was inert until `f910032` on 07-10 — everything deployed before that date is missing). **First active-learning batch**, sampled ABOVE the op-point per ADR-023: **~50 of ~199 articles/cycle reaching readers are off-lens**, precision 1.000/0.705/**0.571** by band, 21 of 34 candidates `solutions_story`. **THE FINDING THAT MATTERS MOST: the oracle graded uplifting's ≥5.5 band 29/29 PERFECT while readers flagged 6.85/6.49/6.09 in it** — an oracle sharing the student's editorial line is blind to a blind spot it shares, so **5 hand-checked reader flags yielded 3 accepted adverse rows against 34 oracle candidates yielding none**. Reader flags adjudicated by owner: 3 accepted (belonging 1→2, uplifting 2→4), 1 rejected, 1 open. `uplifting v7` has its **first accuracy record ever** (recall 0.7361 / spec 0.9189 — best recall, **loosest gate**, 8.1% FPR vs 2.8%/2.1%; *mechanism* for NM#306's corrupted bodies crossing at 38.3% only there). **#81 REPOINTED**: sklearn now 1.8.0 both boxes; the live mismatch is **ST 5.6.0 (sadalsuud) vs 5.2.2 (gpu-server)** on the mpnet+sklearn-MLP detectors — **obituary enforces at 0.85 with a 0.0012 margin, unmeasured**. **|0.16| cross-box skew SCOPED** — does not apply to the e5+torch path (max |Δ| **4.2e-6**, zero flips); b650 cleared for probe training but **cannot run the Gemma student on GPU** (triton/libcuda). **Traps: gpu-server's system `python3` is NOT production** (read `systemctl cat`); `git check-ignore -v` exits 0 on a *negation*; source-prefix id matching grabs the wrong article; excerpts are not sufficient for adjudication (3 of 5 drafts moved on full text). **NOTHING DEPLOYED and nothing needs to be.** Framework v1.18.0, no drift. Issues #81/#90/#91/#96 updated, **#102 filed**. **NEXT: #102** (uplifting specificity, ADR-021 gate on 4.5), then adjudicate the 21 `solutions_story` candidates.
- [2026-08-09 later](project_session_2026_08_09_later.md) — **DIRECTION: the goal is releasing ovr.news publicly, and the owner's standard is "getting the pipeline right is the most important thing, otherwise I will not launch" — pipeline correctness IS the gate, not a competing priority. (The agent first over-corrected into "do an ovr.news issue triage instead" and was told so.) **NEXT SESSION IS ducroq/NexusMind#306 ON ITS OWN**: enrichment replaces correct article bodies with unrelated wire/widget content — 0/725 at collection, 0/300 pre-enrichment, **282/1550 (18.2%) post-enrichment** for one outlet. It reaches the FILTERS, so lens scores are computed on the wrong article; invisible to faithfulness checking BY CONSTRUCTION. **Found by the FluxusSource session, not this one.** Corroboration panel is unaffected (reads pre-enrichment; "Adnkronos" in 3 of 183,010 staged rows) and story dedup is title-only. **BOTH HALVES NOW MEASURED.** Rate (NexusMind session): **~1% corpus-wide**, 510 of 660 sources at exactly 0, 25–82% on affected ones — and **95% of the alarming 22% was NM#276, fixed 07-26**. Harm (this repo, `solutions v6`, two runs, **zero id overlap**): **5.4% of corrupted articles flip a surfacing decision vs 0.4% of controls**. **My run-1 conclusion was REFUTED by my own run-2** — I said the direction was one-way suppression off n=1; run 2 found **4 wrongly PROMOTED**: riots in Paris, a US airstrike with 30 civilian casualties and a stabbing murder all cleared the constructive-news bar because they were scored on a Davines cosmetics release (3 share the IDENTICAL body). Guard now 47/47 on il Fatto. **Also: #276 was never fixed for non-English sources** — the keyword list is English-only, 25.9% of the residual is foreign-language consent walls (NM#292 axis). **RETRACTED, my worst error of the day**: I cleared the FS#144 ordering constraint on a **Sunday sample with 0 arXiv rows in 6,222** — arXiv doesn't announce at weekends — having committed that exact bimodal-weekday lesson hours earlier. Correct order: investment_risk onto `primary_literature.detected` FIRST, then delete the label. **Framework v1.18.0, current, no drift.** Owner declined a proposed working-rule promotion with "will it help my goals?" — it would not. **Do not re-propose it.** Honest ranking: **PR #299 (deployed) helps** (~392 more articles/day; survivor selection fixed — 216 cases where a `news.google.com` redirect beat the real publisher, which is page quality); **NM#301's "N related sources" is a judgement call** (87.7% of what it counts is one wire story republished); **NM#306 is reader-visible** (corrupted bodies miss the panel but reach the FILTERS); **INST-10/PROP-1 helps not at all** and was most of the session. Next session should probably be a launch-readiness pass over ovr.news's ~89 open issues, not more corroboration research — note **ovr#304**, a *publication gate* contradicting ADR-022. Sent to certify one instrument; the owner's own question was worth more than the whole brief.** INST-10 **CERTIFIED** by a non-author (reproduced exactly: unweighted AUC 0.798/0.767, 6/6 strata, ESS 36.1, robust to the date-only confound) and registered in V&V §2 with INST-11 — **but the certification's finding is a REFUSAL.** "Certify INST-10 and turn time on" was two gates presented as one: PROP-1's pre-registered falsification is a **recall** test at fixed cluster size, INST-10 is precision-side-only by construction, and **that test has never been run**. Second blocker nobody had stated: **`config/app.yaml` ships `sigma_hours: 72.0`, the refuted value** — at σ=72 the term nudges 99.1% of true pairs *and 79.4% of false merges* upward; two-sided window is σ ∈ [6,36]. Then the owner asked *"FluxusSource does dedup, NexusMind does corroboration — are we on the right level?"* → **THREE source-blind deletion points in series**, and the biggest had a **merge-ready fix sitting six days past its own expired hold**. **PR #299 MERGED** (`8ed8139`, closes NM#296): the load-time `duplicate_title` drop runs *before a single embedding is computed*, kills **5,405/cycle at ~46.7% cross-outlet**, picked survivors by **file order** (216 cases where a `news.google.com` redirect beat the real publisher), and **has been censoring the corroboration programme's own measurement population** — every registry number including 0.173 is computed on a corpus with its easiest positives deleted first. Rebase was one doc row; tests 1021 → **1038**. Third point, still open: `story_dedup` **deletes ~1,500/cycle AND emits the corroboration count from one pass at one threshold** — opposite risk profiles, error rate unmeasured. **Three claims this repo gave the owner were FALSE**, two of which drove decisions: FS#143's "100% of the class / 0 titles in ≥2 category feeds" (the category feeds duplicate **each other 738×/7 days**; **123 titles were unique** to the dropped feed), and "Contract A is `additionalProperties: false`" (`metadata` is open in both). **NOTHING DEPLOYED. NEXT: deploy #299 and read `cross_outlet_title_kept` (expect ~2,000–2,500, not 0); then run PROP-1's real falsification on b650.**
- [2026-08-09](project_session_2026_08_09.md) — **the change I was sent to ship was already refuted, and nothing shipped.** The brief said the 340-pair panel was UNADJUDICATED; it was adjudicated 2026-08-06 and the verdicts sit in a **gitignored NexusMind dir never copied to b650** — `enumeration-is-not-inventory`, on a machine. Registry PROP-6 already said `title_body@0.96/0.92` **DOES NOT SHIP** (rejects 70% real at its own margin). Added the missing turn-over point on a fresh 680-pair panel: **`0.94/0.90` passes all three pre-registered gates but only TIES live under the #95 standard** — the live baseline itself moved **0.283 → 0.173** at n_eff 36, and **my D2 was weaker than this project’s own rule** (CI bound vs bare point estimate). `title_body@0.92/0.88` decisively dead (**17.7M merged pairs**, giant stratum 0/25). **The real lever is a GATE**: primary literature is **20.6–30.4% of all merges and 0–14% correct**; excluding it beats any threshold move (0.344 → **0.459**). Traced end to end: **FS#143** (arXiv `Announce Type:` prefix shifts `content[:500]` so the collection hash never collides — **600 dupes/48 cycles**, all same id+version; owner’s fix beats mine: **drop the plain `arxiv` feed**, 100% of the class for 77 papers/8 days), **FS#144** (308 GN feeds collide on `google.com`; **86% of `academic` is Global South news**, and the root defect — topic read as source type — survives retiring GN), **NM#305** (article-derived detection: **1.000 recall, 0.000 on Guardian/Ars/Smithsonian/STAT**; Contract A is `additionalProperties: false` so collection-stamping needs a schema change). **STAGE TRAP**: the arXiv prefix is 91.6% at collection and **0.000 after enrichment** — measure a feature where it will run. Features probed on 599 adjudicated pairs: **shared numbers REFUTED** (0.503–0.581), **time CONFIRMED 0.809**, independently replicating INST-10 and **still `temporal.enabled: false`**. Five of my own errors caught by checking (a 91% cross-language figure that **does not replicate** — 2/4 on v3; Rogan-Gladen printed >1.0; "no callers" grep excluded the defining file). **Nothing deployed, no production config changed. NEXT: certify INST-10 and turn time on; NM#305 stage decision; deepen panel-v3’s giant stratum before any threshold flip.**
- [2026-08-08](project_session_2026_08_08.md) — **two halves; read both.** AFTERNOON: **NM#300 was FIVE allowlists, not two** — the morning's fix was deployed, provably loaded, and the next cycle still read **0 of 2,170**. The missed hops sat on the *response → result-object* boundary; the earlier "no third drop" checked the *article → disk* seam, true and irrelevant. Now **100% populated, all six filters; NM#300 + LD#88 CLOSED**. Unasked: **no surfacing article is ever probe-scored** (`surfacing AND stage1_low` = 0 everywhere) — the hybrid design's core safety claim, assumed since 2026-02. **I took the pipeline down** by deciding to KEEP `.bak` files under a deploy-gated path for an unverified fix whose commits were already pushed. **cd v6 unblocked but deliberately NOT deployed.** **LD#93 step 4: do not set the cap** — and my residual was understated 3× and my mechanism refuted, both by the FluxusSource session. Shipped NM#303 → **NM#304** (Contract A had never met production either), census hardening, `commit-msg` fix, framework → v1.17.0, **agent-ready-projects#33**. MORNING: **three of my instruments broke and none of my conclusions did.** LD#101 **confirmed by outcome and CLOSED** (eval rows **21 → 0** in all six filters, with the control that the same cycle's input carried 22) — but the check I pre-registered for it (`excluded N` "must exceed 121") read **86** and would have called a working fix broken; it aggregates all types and swings 69 → 121 → 86 in a day. The fix was also living as **uncommitted edits on one box** — nothing in NexusMind referenced `eval_aggregator` — now `9fb441a`. The 30 published rows were **not** a cleanup job: all inside the 10-day window, ageing out 08-09..08-14, and mostly good global-south coverage; **4 defective ones suppressed** via the config kill switch (`ovr.news@75bde57`), proven by executing the rule with negative controls. **NM#300 = two drops in series** (`FilterScoreResult` Pydantic allowlist kills it first, then `main.py`'s `analysis` allowlist) — both deployed free, since the scorer was down with ollama holding the GPU; **unproven until the 12:00 cycle**. **The contracts had never met a production row**: first run over 2,400 live rows gave **908 violations** — `image_confidence` declared 0–1 is a **raw logit** (−12.33..6.37, 68.4% outside); contract wrong, producer right, and fixtures could never have caught it. **908 → 1**, that one real and left failing. Built `scripts/stamp_census.py`, which **failed its own acceptance test first** (missed NM#300 — absent ≠ null — and LD#94 — six filters averaged hide a single-filter constant). **Chain 8 CLOSED by a parallel session** (ADR-007, FS#120) 30 minutes before I commented on it asking for a decision already taken. Board **196**, no calendar-bound item left. Filed NM#303, FS#138.
- [2026-08-07 night](project_session_2026_08_07_night.md) — **the dedup question answered by reading the call path, not by waiting a month.** FS#133's answer was **RETRACTED the same night by the adversarial lens**: I concluded "arbitrary" from the n=2 the issue explicitly says not to conclude from, and my premise ("both drops were inside one source") is an **instrument artefact** — only **10.1% of hashes carry a source**, so cross-run drops are undetectable. Cross-run loss is in fact **cadence-driven and publisher-correlated**, and **sticky for 30 days**, not a coin flip. The question is open until ~09-06. **FS#120 (due ~08-14) is partly measuring noise**: `newsdata_eval`'s **local-publisher share is 40%/12%/8%** — and that is probably H3's *answer*, not a defect (**I first called it 77–97% off-topic and prescribed `country_queries`; both were wrong** — NewsData filters on publisher location, so the fix would have destroyed the geographic-vs-topical comparison), `items/day` is censored by a 30/run cap, and my GDELT effect was **refuted twice** — a 72.6% figure that straddled a fix boundary, then a "76%→66%" whose sign is backwards (full record: **66.4% pre → 76.9% post**, Fisher **p=0.546**; I split on the GitHub *close* time, not the deploy time). FS#125's *coverage* half is real; its *yield* half cannot move — external per-IP quota. Cross-repo audit: **13 open issues cite closed dependencies**, two of them mine from the previous session (FS#133/#134 → the closed NM#213). Board: NM#225→Chain 15 (**re-dated, 71 days old**), NM#226→13, NM#254→16, Chain 17 promoted, **198 open not 195**. FS#134 → **delete**. **LD#101 filed + closed out 08-08**: the FS#120 eval arms were scored and **published** (30 rows in ovr.db, a funeral story at tier `high`) because `type_classification: eval_aggregator` had **zero consumers** — now excluded in all 6 live filters, verified by *executing* the guard, not by reading the key. *(My first recommendation there was also wrong — I proposed building a gate in ovr.news that `excluded_source_types` already provides, contradicting a memory file I own.)* **NEXT: give FluxusSource its own session; confirm the eval exclusion on the next cycle log.**
- [2026-08-07 late](project_session_2026_08_07_late.md) — **a coverage pass, a plan that lost, and a drop nobody had counted.** Board unchanged at **195**, so the value was in what it does *not* cover: **FS#85 is closed and re-homed to NM#232**, which is open, untouched 54 days, has **no NER anywhere in the pipeline**, and blocks **four dependents banded nowhere** — two of which (NM#223, ovr#222) still cite the *closed* FS#85 and therefore read as unblocked. **Chain 15 is 71 days old, not 2** (NM#225 stated it on 05-28, unbanded, and is the most actionable of the three). NM#254 is Chain 16's missing link. A **seventh repo takes mandated output and is on no board**: augmented-engineering, 34 open, **1 closed ever**. Then wrote a full NM#232 plan and ran a **six-lens multi-model battery that refuted its central recommendation and three of my stated facts** — the "cheap alternative" is unbuildable (**85.5% of credited sources are an integer with no record**) and was already measured as anti-signal; **I picked a display-layer consumer over the only one with code**; Dutch has the full label set after all (but the *worst* accuracy); the GPU-contention worry was fictional; and my superset numbers came from a glob that skipped 30 days of rotated logs. Nobody but the completeness lens noticed **persisting a searchable named-person index is plausibly a new GDPR processing purpose**. **SHIPPED + DEPLOYED**: FluxusSource `4994d61` — collection dedup drops on `md5(title + content[:500])` **with no source comparison**, so the same wire copy from two outlets is deleted before NexusMind sees it (**FS#133**); now counted, DEBUG→INFO, drop unchanged. **FS#134**: MinHash implemented, `datasketch` pinned, **zero call sites**. **NEXT: read the cross-source count off the 20:00+ runs (a floor until ~09-06) — it gates the corroboration track's step 3, which mines a corpus that may be pre-depleted.**
- [2026-08-07 evening](project_session_2026_08_07_evening.md) — **chain re-verification, a new repo, and a review battery that gutted it.** Every chain link re-queried: **two marked ✅ were open** — LD#73 (bookkeeping, closed) and **NM#185, which is not**: Chain 1 is *not* complete, it carries an unstarted **commerce prefilter v3** whose evidence base was 100% `sustainability_technology`, deleted 08-03. **persuasion-scorer's 12 issues were counted in every board total and banded nowhere** → Chain 16. Built **`veen-systems/pipeline-atlas`** (6th repo): whole-chain architecture as a *signal path*, interactive drill-down diagram, and a **level-diagram ops snapshot**; served from sadalsuud **over Tailscale, not GitHub Pages** (free plan = public repos only). ovr.news `/ops/architecture` deleted. **Then six reviewers found 15 FALSE CLAIMS in a site whose only job is being true** — including a gate diagram that reproduced the exact bug NM#281 fixed, and **two verify commands returning confident false all-clears**. Plus: the docs repo was an **RCE path** onto the pipeline box (quarto `pre-render` runs shell, unhardened, beside four passphraseless SSH keys); a nonexistent systemd unit rendered **identically to a healthy stopped one**; my own font-strip guard **aborted the run when it succeeded**; hardening then **broke the server** (`tailscale` needs AF_UNIX) while `is-active` said "activating"; and the page **rendered blank** from an apostrophe I introduced. All fixed, all proven by execution. Smoke test added.
- [2026-08-07](project_session_2026_08_07.md) — **six owner decisions walked one at a time; two were STALE AS LISTED** (already decided in a repo the board does not read). Then two review rounds found **5 code defects and ~20 doc errors, nearly all in the same day's own work**. **The signature defect hit occurrences 6 and 7, and both had CORRECT CALLERS** — one guard was reverted by a later pipeline step through a `COALESCE` merge (123 stored rows already carried the signature), the other was a **complete no-op** proven only by *executing* it. The CLAUDE.md working rule is escalated accordingly: name the caller **and prove the outcome changed at the end of the run**. Also: decision 4 left a reader-visible wrong hero (normalized 9.10) because I reasoned about a group's date range instead of computing per row; **corroboration precision is 0.560, not the 0.283 headline** (cap-disabled, and the gloss inverted its own evidence); and **ovr.news never reads NexusMind's `display_rank`**, so `1bbadb5` did not fix the ranking half — **decision 7 was created by the review and TAKEN the same day** — ovr's own 1.3/1.5/1.7× boost **bounded to a flat 1.3× for 2–10 total sources** (`1ecf853`); the ladder was removed, not retuned, because precision is non-monotone in cluster size. Correcting its published page turned up a second live defect: **decay published as 0.95 against a configured 0.85** (0.70 vs 0.32 at 7 days). **All seven decisions taken; everything pushed. NEXT: the R2 round-trip for 6 uncleared rows, the #284 exposure window, FS#120 ~08-14, and re-measure corroboration precision on the CAPPED system ~08-18.**
- [2026-08-06 evening](project_session_2026_08_06_evening.md) — **decision-bound: four owner calls closed three backlogs.** **#95 step 2** — "pin a batch size" was never available (`DEFAULT_BATCH_SIZE = 16` is already fixed; the variable is batch *composition*), so the 0.16 floor became a **band the deploy gate prints**, and two models whose bands overlap are NOT DISTINGUISHABLE — **this is what unblocked #87 and #93 step 4**. **cd v6**: criterion 4 executed (prefilter 800 → ~90 lines) *and* full package parity — three inference modules, v5's calibration, `score_scale_factor` 1.2829 → **1.0** (the one live hazard), gatekeeper removed (#94); verify 7/7; blocked only on a Hub repo that does not exist and an unfitted `normalization.json`. **#94** static invariant shipped (`GATEKEEPER_CAP` < medium threshold, read off the scorer class) — catches cd v5 *and* solutions v6. **#97**: models clean (zero `User-agent: *` reservations), but **812 committed rows of full article text in a PUBLIC repo** — republication, which grounds 2–3 do not cover — truncated to 300 chars (**1,889,627 removed**), though git history keeps it. **ADR-012 rename backlog CLOSED**: cd + nature_recovery keep their names, `solutions` confirmed, `uplifting` → **`human_thriving` at v8**. #88 items 2–4, #84 v7 corrections, #99 closed by removal. **Two of my own errors caught by self-review** (a wrong count in the ADR; a commit that landed on the parallel session's branch). **NEXT: cd v6 cutover on gpu-server, then #87.**
- [2026-08-06 afternoon](project_session_2026_08_06_afternoon.md) — **#98 cd v6 probe shipped** (held-out oracle FN 0/75 vs the keyword gate's 10/75; production surfacing blocked 337 → 1 of 2,653; threshold 2.50 not the trainer's 3.025, because val FN is optimistic by construction). **The review battery found 2 blockers in the same day's work, both mine** — v6 ships a config block and probe into a package with no inference module and no calibration.json (`_load_calibration` fails silent), and a val-vs-production rate comparison written *while correcting an identical one*. Also: **#99** filed (English-only escape hatch), **FS#120 answered + harness built** (pre_enrich fires at **500**; GN proxy enrichment succeeds **0 of 14,198**; 240 of their 312 GN sources are single named outlets that shouldn't be in the 08-14 gate), **ducroq/NexusMind#300** filed (#93 stamp never reaches disk), context audit run, four SSH assertions cleared. **NEXT — the bottleneck is three OWNER DECISIONS, not work: #98 criterion 4, #94, and #95 step 2 (make this one first — #87 and #93 step 4 are both stuck behind it). Work I can do meanwhile, in order: #97 (~30 min, closes a compliance loose end), #88 hygiene, #84. Leave alone: #99, #87, #93 step 4, #90, and v6's package gaps. FS#120 needs nothing until 08-14, then one command.**
- [2026-08-06](project_session_2026_08_06.md) — **four-lens review of the previous evening's commits: 4 blockers, 13 warnings, all fixed** (`b37b88e` here, `8ce017c` in ovr.news). **Every measurement held; the prose around them did not** — a duplicate footer link live on every page from a codebase fact asserted without a grep, `GPTBot 401 domains` published against a stated total of 333, ADR-003 contradicting itself three ways, and a guard test left stale while the table it enforces grew (the whole art. 50 marker was deletable with a green suite — now proved to bite). **Also: `~/.claude/skills/review-changes` pointed at `repos/personal` and silently won over this repo's own adapted copy — a global skill shadows a project-local one of the same name with no warning.** Symlink removed, two stale local copies deleted, four improvements ported in. A second session was live in ovr.news throughout; nothing of theirs was touched. **NEXT: FS#120 (~08-14).**
- [2026-08-05 evening](project_session_2026_08_05_evening.md) — **board refreshed (191 open, two banded P0/P1 entries were already closed) and the legal arc executed, almost all of it in ovr.news.** Five owner decisions recorded: oracle-to-third-party risk **knowingly accepted**; AI-crawler opt-outs **don't bind** the fetcher (ovr ADR-043); training is *"modelling, not mining"* (recorded here, with the counter-argument against itself); disclose on cards **and** search snippets; publisher is **Veen Systems**. **Two findings changed the work: the EMFA micro-enterprise exemption does not exist** (zero occurrences in the adopted regulation — ovr.news is probably in scope and has been since Aug 2025), **and the site published a data controller that does not exist** (Busara.eu). AI Act art. 50 now covered on every surface; EMFA art. 6 ownership published; GDPR art. 33(5) incident record written. New **Chain 15 (lens commensurability)** — ovr#296's 0.043 margin sits inside #95's 0.16 noise floor. **NEXT: FS#120 (~08-14).** Nothing committed.
- [2026-08-05](project_session_2026_08_05.md) — **LD#92 IDENTIFIED**: D3 (matched percentile depth) −1.119 is the *largest* effect, not the collapse the selection artifact predicted; gemini cross-check −1.351 kills the "judge penalises short input" alternative. Cap value now blocked **only** on #95. **`review-changes` skill adopted (framework v1.10.6 → v1.14.0), re-mapped not copied — its first run caught four of my own same-day errors.** GN measured as the stub source (14–17% of articles, 48–56% of stubs) → evidence into FS#120; **ovr#299 filed** (headline-only summaries are 83.4% invented; the model has a fixed length target and fills it). Harness **and fixtures** committed at last. **NEXT: FS#120 (deadline ~08-14), then #95 batch pinning.**
- [2026-08-03 evening](project_session_2026_08_03_evening.md) — **all three junk gates verified against the running box**: obituary enforcing (max surviving score 0.8488, zero ≥0.85), commerce enforcing (LD#80 `v1` pin holds), violence inert by design. **Commerce provenance fix** (86.4% of corpus had a verdict with no model version) and **seeded cycle replay for #95** both DEPLOYED. sustech v3 + foresight v1 REMOVED (#64 closed). Scorer call path settled — `/home/hcl/llm-distillery/` on gpu-server is a stale decoy. **#90 not ready: audit which template elements are load-bearing (#94, #92) before spreading.** **NEXT: verify next cycle (run-seed line + commerce `processed` ≈21,000 once)**
- [2026-08-01 afternoon](project_session_2026_08_01_afternoon.md) — four-repo re-inventory (156 open, **12 chains**, new P0 set incl. LD#91/ovr#284/ovr#285); **persuasion-scorer split out** as a verified system under three agent-ready frameworks (LD#78/#79 externalized); post-deploy checks 1+3 PASS (`_commerce_model: v1`, `violence_blocked` gone from Loaded line), 2+4 pending mid-cycle. **NEXT: finish checks 2/4, then NM#285 measurement**
- [2026-07-31](project_session_2026_07_31.md) — obit carryover diagnosed (47 shadow-era + 2 v5 FNs); LD#76 11-agent audit falsified shared-root-cause; EXECUTED: uplifting+belonging refits, cd topic gate (#86), NM#280 tier fix — all deployed; ADR-022 written; LD#90 harmonization program (renames: thriving/discovery/recovery). **NEXT: verify ~12:00 cycle (3 checks), then close NM#279/#280, LD#74/#76/#86**
- [2026-07-30](project_session_2026_07_30.md) — S1: v4 op-point evidence + swap. S2: deploy verified, FN-delta gate FAILED, DeepSeek-commit review. S3: v5 trained + review battery + June panel, owner adjudication (grief-vs-news), ENFORCEMENT v5@0.85 shipped, b650 commissioned
- [2026-07-28](project_session_2026_07_28.md) — obituary v4 retrain, violence_promotion v1 shadow-deploy, solutions v6 normalization fitted
- [2026-07-27 evening](project_session_2026_07_27_evening.md) — LD#49/#68/#63/#57 closed; NM#276/#206 closed; curate
- [2026-07-27](project_session_2026_07_27.md) — solutions v6 gate PASSED, obituary NM#185 Phase 3 deployed, playbook update
- [2026-07-26 evening](project_session_2026_07_26_evening.md) — solutions v6 train (probe-split retraining, val MAE 0.476)
- [2026-07-26](project_session_2026_07_26.md) — multi-repo triage, cross-repo dependency chains, P0-P4 priority ranking
- [2026-07-22](project_session_2026_07_22.md) — solutions v4 deployed & live-scoring, ADR-020, NexusMind cutover
- [2026-07-21](project_session_2026_07_21.md) — solutions v4 gate, nr v4 ground-truth comparison, calibration fixes
- [2026-07-20](project_session_2026_07_20.md) — solutions v4 hybrid scorer, probe threshold calibration, production prep
- [2026-07-19](project_session_2026_07_19.md) — solutions v4 training, calibration, normalization fitting
- [2026-07-18](project_session_2026_07_18.md) — solutions v4 rename (ADR-012), oracle rescore, training data prep
- [2026-07-17](project_session_2026_07_17.md) — Fix A normalization hardening, Fix B deploy hardening, review battery
- [2026-07-17 evening](project_session_2026_07_17_evening.md) — Fix B EXECUTED: git-archive staging, deploy validation
- [2026-07-16](project_session_2026_07_16.md) — normalization anchor fix (#205 root cause), calibration audit prep
- [2026-07-14](project_session_2026_07_14.md) — climate_doom cap retired, nr v4 op-point wiring, normalization refits
- [2026-07-11](project_session_2026_07_11.md) — nr v4 training, calibration, ground-truth comparison
- [2026-07-10](project_session_2026_07_10.md) — nr v4 deployed, normalization refit, op-point validation
- [2026-07-09](project_session_2026_07_09.md) — nr v4 ground-truth gate, multi-model review, ADR-021
- [2026-07-08](project_session_2026_07_08.md) — nr v4 oracle rescore, prompt fixes, protection scope (#70)
- [2026-07-04](project_session_2026_07_04.md) — nr v4 planning, ground-truth design, oracle selection
- [2026-05-31](project_session_2026_05_31.md) — cd v5 deployed, #62 leakage resolved, DeepSeek oracle reference
- [2026-05-29](project_session_2026_05_29.md) — cd v5 #62 hard-negatives, oracle prompt v5 deltas
