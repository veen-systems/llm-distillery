# Memory Index

<!-- verify: python3 scripts/verification/check_index_budget.py -->
<!-- verify: python3 scripts/verification/check_index_budget.py --target project -->
<!-- verify: python3 scripts/verification/check_index_budget.py --target loaded -->
<!-- verify: python3 scripts/verification/check_index_budget.py --target pointers -->
<!-- verify: python3 scripts/verification/check_doc_claims.py -->
<!-- verify: python3 scripts/verification/check_claim_shapes.py -->
<!-- verify: python3 scripts/verification/check_training_provenance.py -->

⚠️ **This index is budgeted in SIZE, not lines** (bytes — the guard reads `"rb"`; say bytes when quoting it). It reached **54,808 chars in 91 lines** on 2026-08-15 (up 69% in three days) because entries grow by lengthening, so a line-count heuristic never fires. Session detail belongs in `project_session_*.md`; an entry here is a hook that answers *should I open that file?* ⛔ **This file is NOT auto-loaded** — it is reached by a `CLAUDE.md` pointer row. The always-loaded layer is `CLAUDE.md` plus the USER auto-memory index, budgeted together by `--target loaded` (#138).

⛔ **THE SESSION LOG HOLDS FOUR ENTRIES. The fifth is MOVED to [`session-log.md`](session-log.md), never compressed.** (#123, 2026-08-25.) The index was 69% session entries by character and grew ~600–900 per session against a fixed ceiling; hand-trimming recovered ~100 chars per line removed, so it lost roughly 6:1 — and every trim turned a finding into a pointer, which is how the always-loaded layer quietly stopped being a record. **Moving costs nothing and loses nothing.** A revision to an existing entry is as expensive as a new one, so budget for both.

*Standing rules moved to `CLAUDE.md` § Hard Constraints → "Working rules" on 2026-08-06 (context audit): they are always-needed constraints, and this file is navigational. This index is pointers + session log only.*


- **2026-09-08 (second) — the cutover is a 7× cut, and Phase E's rows arrived to a guard that refuses them** ([session](project_session_2026_09_08_second.md)) — **$0**, no oracle calls, no GPU. **EXP-030**, `b28e456`/`9fb3feb`, **not pushed**. New issue **#154**. ⛔ **SUPERSEDES the row below on two counts: the v7-vs-v8 comparison HAS now run, and Phase E is 202/200, not 168/200.** ⭐ **The free comparison: on the same 15,372 production articles v7 surfaces 1,184 and v8 surfaces 168** — a **7× cut**, both 152 / v7-only 1,032 / v8-only 16, Jaccard 0.127, v8 nearly a SUBSET (90.5%) and **no `high` tier at all**. ⛔ **Not a threshold difference, tested against a null that could have said so**: volume-matched recovers 57.5% where a Spearman-matched *noisy-v7* null gives **76.8%**; within-union Spearman **0.3436** vs the null's **0.5630**. ⭐ **44.7% of the dropped 1,032 are already on another lens** (baseline 7.65%), so ~55.3% — 242·128·96·105 per cycle — are surfaced by NO other lens; ⛔ that is *surfacing*, not readers. ⚠️ **Nothing here says which lens is RIGHT** — no labels, and judging the 1,032 is unfunded. ⛔ **PHASE E: 202/200 AND BLOCKED BY OUR OWN GUARD (#154), not by data.** `fit_normalization.py`'s NM#205 check compares `sample_min` (4.5069) against an ABSOLUTE 4.5 that **is** this filter's op-point, so at 4.5 it is a sample-**density** test; its comment names the expired premise (*"no false-block possible for any real op-point (3.75/4.0)"*). **`uplifting v7` passed it at 4.500027 on 15,698 rows and would FAIL a refit on four cycles today** (4.5018) — same filter, same op-point, opposite verdict. ⛔ Fix is TWO files: `tests/unit/test_normalization_invariant.py:190` carries the same bound. Not changed — owner ruling. ⚠️ NM#319: 60.4% would clear the normalized 4.0 gate, ⛔ **in-sample and near-tautological** (normalized 4.0 *is* those rows' 40th percentile). ⛔⛔ **VERIFICATION IS NOT REVIEW, 8th session running, and this time it CHANGED THE ANSWER.** 762 tests + six guards green; four lenses returned **4 blockers, all in this session's own code**. ⭐⭐ **THE KEEPER: §2 mixed two instruments — 3,162 rows carry an e5 PROBE estimate in `raw_weighted_average` — and on the frame I published, the noise null gives 0.3654 [0.3281, 0.4172] against an actual 0.3463, INSIDE THE NULL. The conclusion was right; the evidence for it was not.** And the control written to catch exactly this was a **`print`, not an `assert`**, inside a section headed *"they run in `compare.py` and assert"*, with a gloss true in its first clause and false in its second. Also: **"Spearman 0.224" was not Spearman** (global ranks correlated inside a subset; it is 0.3463); a **6-vs-56 denominator** made a count split point the wrong way (33.3% vs 17.9%, p=0.328); and *"55% would leave ovr.news entirely, ≈143/cycle"* stated surfacing as reader-facing and averaged in the backlog cycle. ⭐ **What found the keeper was building the null nobody had built — mutating the world the code runs in, not re-reading its lines.** Corrections published to #151 and #154; four superseded surfaces fixed. ▶ **NEXT: #154 is an owner ruling, and the cutover ruling (#151 step 5) now has its numbers.**

- **2026-09-08 — v8's first live-output audit: the student is faithful, the PROMPT is not** ([session](project_session_2026_09_08.md)) — **$0.16**, 6 DeepSeek passes + 2 rubric arms. **EXP-029**, #153. Scope precision **60/62 = 0.968** vs the gate's 0.850, not distinguishable; only **2 of 62** off-lens. ⭐ **Swapping ONLY the prompt moves 12 articles out of `in_scope` and 1 in (paired McNemar p=0.0034) — and those 12 are `STATUS.md`'s three OWED v8.1 gaps**, rediscovered from production by a route that never read the prompt. Truncation a clean null; family n.s. ⛔ **Every null is a RATE-null with case churn** (κ 0.587, 8 of 62 disagree at a −0.032 rate). `[4.5,5.0)` holds 58.7% of volume and every instrument's errors — **but do NOT raise the op-point**: 7 of 12 are a known prompt problem, and a panel selected on passing cannot measure what a higher bar would lose. ⛔ Phase E **168/200 cumulative**, don't disable v8. ⭐ The v7-vs-v8 same-articles comparison is **free and never run** (#151).
- **2026-09-07 — v8 DEPLOYED and ENABLED, and the deploy that called nothing** ([session](project_session_2026_09_07.md)) — **$0 spend**, no oracle calls. **EXP-028**, `006b87d`/`893ec5f`/`62d2089`; NexusMind PR **#452** (`e0f0af9`) then **#453** (`9245f2c`). Issues **#150**, **#151** filed; **#147** commented. ✅ **`human_thriving v8` is deployed AND enabled** — adapter b650 → Situla → gpu-server (sha256 `074209ff…` read back at all three hops, 0 of 3 differed), Hub backup published private with `--selected-epoch 5`, `inference_hub.py` landed and `NO_HUB` deleted in one commit, smoke suite now covers **six** filters. ⛔⛔ **THE KEEPER — PR #452 PASSED EVERY GATE AND DEPLOYED A FILTER NOTHING CALLED.** I published *live in production* on the strength of a direct `POST /filter/human_thriving/score` returning wa 5.604 → medium. `human_thriving` was never in `pipeline.enabled_filters`, the list `scripts/main.py:2569` iterates, so no cycle would have touched it, `data/filtered/human_thriving/` would have stayed empty **forever**, and Phase E could never have run. ⭐ **I tested the CALLEE and inferred the CALLER** — the outcome check I ran deliberately, to satisfy the working rule, answered *can it score when asked* rather than *does anything ask it*. ⭐⭐ **Found by writing an unrelated smoke fixture**, which forced a read of `NexusMind/scripts/deploy_filters.sh`'s alignment gate; the post-deploy smoke suite had passed the whole deploy **having never loaded v8**, having no fixture for it. ⚠️ **A NEW filter has an enablement step a version upgrade does not** and every guard was built for the upgrade case. 21st occurrence of *prove the outcome changed*. ⭐ **EXP-028 / `H-V8-26` REFUTED**: the gate's precision 0.550 is the PANEL's — design-weighted (25.1×, positive rate 5.3030% → 3.1638%) it is **0.6073** [0.5524, 0.8159] with specificity **0.9941**; weighting was expected to hurt precision and helped it, the FP rate falling 1.44% → 0.59%. ⚠️ Bands overlap, so not a distinguishable difference. ⭐ Under that mix calibrated beats raw on specificity with **disjoint** bands — an ADR-023 argument invisible to `calibration_report.md`, whose verdict is in MAE. ⛔ **Four published numbers corrected**: `CLAUDE.md` still carried v8's superseded epoch-4 gate (0.343/0.992 → **0.314/0.9856**); the *3 to 8 false positives* band was epoch 4's (epoch 5 is **5 to 10**); `check_claim_shapes.py` rejected my own unbanded *beats raw on BOTH criteria*; and I corrected a peer with **8** ovr.news files when it is **12** — my `--include` dropped `.astro` and `head -15` cut three more. ⭐ **Cross-session coordination caught what neither session's checks would**: `nexusmind-0a` had recorded *"do not flip `enabled_filters`"* before my message, which would have told the next session to block PR #453. ⛔⛔ **AND THEN THE ENABLEMENT TOOK THE PIPELINE DOWN.** The first post-enable cycle ran the **SHARED** preprocessing stages 7–10× over normal (og:image backfill **21,245** vs 2.6k–3.1k; hero extraction **41,435** vs 3.3k–4.0k; ML candidates 18,206 vs a 3,000 cap) and was on course to be SIGKILLed at `TimeoutStartSec=4h` **before scoring started** — zero output for **all six filters**. **Cause: a COLD START, not the prefilter.** A new filter has no `data/raw/.processed_ids_<name>.json`, so it loads the whole `max_article_age_days: 3` window (~18 cycles ≈ 41,400 vs the observed 41,435), and dedup + image analysis run on the **union** of every enabled filter's pool (`scripts/main.py:3555-3567`, skip list at `:1565`). ⛔ **It does not self-heal** — `_save_processed_ids` is at `:2141`, after the scoring loop, so a kill leaves the file unwritten and the next cycle repeats: a **kill loop every 4h**. ⭐⭐ **THE KEEPER, from the `nexusmind-0a` session: the COST and the THING THAT WOULD END IT are on OPPOSITE SIDES OF THE SAME TIMEOUT** — *a one-time cost whose receipt is written only on success is not one-time, it is a loop.* ⭐ **Two mechanisms fitted the same 10×; only one survived reading the code** (the peer's prefilter hypothesis fits the magnitude and is refuted by the path). Mitigated by the PEER, owner-authorised, 15:27 — stop, seed the file from `uplifting`, `reset-failed`; **my contribution was the detail that changed their command: copy WHOLESALE**, because the `versions` sidecar is filter-agnostic corpus data (#119) and copying only `ids` would have silently disabled change detection for v8. ⚠️ **That seeded file IS the mitigation — deleting it restores the outage.** I stood down and did not touch sadalsuud. Written up: `docs/RUNBOOK.md` § **4b** (a step for a NEW FILTER NAME, not a version bump), `#152` (guard proposal, with the open question that enabling happens in a NexusMind PR so a guard here would not fire), `H-V8-27`. ⭐ **BOTH of today's misses are one shape, one layer apart: I verified the thing I CHANGED, not the system around it** — first that nothing called the filter, then what its first cycle would cost. ▶ **NEXT, BEFORE ANY OTHER WORK: the acceptance test is UNREAD.** Unit was `inactive dead` at 15:41, next collection 16:04. Check og:image back to **2.6k–3.1k** and hero to **3.3k–4.0k**; **fail on either ⇒ back `human_thriving` out of `enabled_filters` and re-diagnose, do not guess a third mechanism.** ⚠️ **v8 has still never scored a production article.**
- **2026-09-06 (second) — phase 9: a RETRAIN instead of a provenance exception, and review found 9 blockers on a green tree** ([session](project_session_2026_09_06_second.md)) — **$0 spend**, one ~95-min GPU run. **EXP-027**, `64b469d`/`c0127d4`/`0d7115a`/`67c6f59`. Issues **#148**, **#149** filed. ⛔ **OWNER RULING, verbatim: *"no exception, i want this system to be harmonized"*** — v8's adapter was orphaned by `git commit --amend`, and it was **RETRAINED under `64b469d` on `main`** rather than shipped with a carve-out. ⭐ **It was worse than "unrecorded": b650 was a partial rsync, not a checkout, and its `train.py` was the PRE-FIX version missing the 176 lines of checkpoint-selection machinery `main` says trained the artifact** — the box did not hold the code the record named. Now a real checkout; 12 files had drifted and `main` was byte-identical to sadalsuud, so the fix moved b650 **toward** production. ⚠️ **The retrain is NOT the same model**: `recall_medium` saturated in EXP-015 (tie-break kept epoch 4) and did not saturate here, so **epoch 5** was chosen outright. Gate **recall 0.314 / spec 0.9856 / precision 0.550, 20 surfaced, 9 FP** against 0.343 / 0.9920 / 0.706, 17 / 5 — ⛔ **all four #95 bands overlap, NOT DISTINGUISHABLE**; the adverse direction is a direction, not an effect. ⭐⭐ **THE KEEPER, and it answers the owner's own question about precision**: classifying every FP by the oracle's `scope_verdict`, **6 of the 9 are `in_scope`** — thriving stories scored just under 4.5 — so genuinely off-lens is **3 of 20** (off-lens precision **0.850** vs 0.882) and **category errors are 1 either way**. The plain FP count moved **5× more than the reader-facing quantity**; specificity 0.9856's own #95 band is **5 to 10 false positives** (⚠️ corrected 2026-09-07 — 3 to 8 was the SUPERSEDED epoch-4 band). `H-V8-25`/#149. ⛔⛔ **VERIFICATION IS NOT REVIEW — 7th session running, worst margin yet: 746 tests + five guards green, six-lens review returned NINE BLOCKERS, all in that day's own code.** Three were the new guard failing at the thing it was built to catch — `git branch --contains` prints `* (HEAD detached from …)`, truthy after `lstrip("* ")`, so the "commit on no branch" refusal **never fired**, and **my test checked out `main` first**; `git -C` walks up, so a nested copy stamped the enclosing HEAD, and **my test used `tmp_path`, outside any repo**. ⭐ **Two of the three were found by mutating the WORLD the guard runs in — a detached HEAD, a nested directory — not its own lines.** Also: nothing *invoked* the checker (delete-it mutation survived; now a `verify:` annotation), the baseline could **grow** to silence a failure, and the blast-radius mutation (deleting `**git_provenance`) passed all 746. ⚠️ **Doc-accuracy found the superseded 0.343/0.992 still published on four fleet surfaces the session never opened, `CLAUDE.md` included** — updating a filter package does not update the documents that quote it, and the commit message claimed it had. ⚙️ Also: **`README_MODEL.md` was gitignored repo-wide** (five on disk, none tracked, any filter); `filter_completeness.py`'s list still named a package deleted 2026-08-03; a published MAE pairs an **unclamped** before with a clamped after (production's arm is **−3.6%**, not −2.0%). ▶ **NEXT: the weights have not moved** — adapter to **gpu-server**, Hub publish, then `inference_hub.py` + delete `NO_HUB` in one commit, then deploy, then Phase E.
- 📓 **Older sessions — [`memory/session-log.md`](session-log.md)** — every entry before the four above, VERBATIM and unabridged (**55** as of 2026-09-06 — `grep -cE '^- ' memory/session-log.md`, do not quote this from memory). The index keeps the newest four; `/curate` MOVES the fifth into the log rather than compressing it, so no finding is ever shortened to buy space. **Look there before concluding something was never recorded.**

- [Date errors & the recency boost](date-error-recency-boost-hypotheses.md) — a flat recency boost on `published_date` means **any** date error landing a row inside the window wins it, invisibly. Both hypotheses RESOLVED. ⚠️ **`collected_date − published_date` is NOT a fabrication instrument without a `source` breakdown** — a fixed-publication-moment source impersonates the signature once per daily cycle. Read before touching dates, the boost (`NexusMind/src/scoring/display_ranking.py`), or any age-derived figure.
- [Hypothesis ledger](hypothesis-ledger.md) — ⭐ **START HERE to recall prior work.** Every hypothesis, its verdict, and where its experiment lives, across 10 files, two repos. ⛔ **`H4` is defined in FOUR files; three files have no ids** — always qualify. Corroboration hypotheses live as `INST/OBS/ART/PROP` rows in NexusMind's V&V registry, the only place recording instrument **certification**.
- [Corroboration feature hypotheses](corroboration-feature-hypotheses.md) — the cosine/time/NER feature set **already exists and was already measured**; what is confirmed, refuted and untested, with the values. **The threshold is not the lever** — the biggest one is a gate, not a feature. Shared-number features refuted; time confirmed and still switched off. Read before proposing any NER or matching-feature work.
- [Google News corpus hypotheses](google-news-corpus-hypotheses.md) — the largest single population in the corpus, and **every number there carries its denominator because four denominator errors were made deriving it**. Five claims already REFUTED. ⚠️ **Never oracle-re-score a GN row**, never match GN on a `gn_` key prefix, and always name whether you mean feeds, rows or items — they differ by up to 5×. Read before quoting **any** GN number.
- [CD v6 probe hypotheses](cd-v6-probe-hypotheses.md) — #98: what the e5 probe confirmed (per-language gap gone; batch-invariant, unlike #95), what it refuted (screening is a REGRESSION; 4 of 5 "recovered" positives are off-lens), and the traps (v6 cannot score — no inference module, no calibration)
- [Cross-repo prioritization](cross-repo-prioritization.md) — master issue landscape across 7 repos. ⛔ **No board count is stated here or at the top of that file — run the `gh` loop at the head of it and quote the result with a timestamp** (removed 2026-08-17, deliberately; do not restore). **The count is never the finding, the distribution is**: a small minority of open issues is touched in any two-day window, ovr.news is the largest board and the least active, and two boards are frozen — all of it re-derivable from that one query. Also holds the four live chains, with the FS#145-is-an-enabler and NM#314-is-built corrections.
- [Stamp & contract integrity](stamp-contract-integrity.md) — **the stamps are a per-article feature set (~40+ numbers), and nothing validated them.** The schemas check SHAPE only and are permissive. **Read before adding a stamp or trusting a stamped field.** ⚠️ **The contract is being REDESIGNED, not patched — read `docs/proposals/contract-a-redesign.md` first; #112 supersedes #111.** **The rule: a field belongs in Contract A iff only the collector can know it AND it is destroyed if not recorded now.** ⭐ **Greenness is not evidence a schema tracks reality.** Also holds the five instrument traps (the validator counts **errors, not rows**), the seven exclusive+perishable categories (**G, the non-event, is the one no symptom surfaces**), and two **superseded** counts kept to date their correction — never re-quote either.
- [Opinion/editorial genre hypotheses](opinion-genre-hypotheses.md) — #121 over 233,338 rows: the within-source control **dissolves the effect in 5 of 6 lenses**; only `investment_risk` survives, and a topic confound survives inside each source. The numbers and the op-point trap are in the `CLAUDE.md` pointer row; the derivations are here. Read before quoting any #121 number or building a genre stamp.
- [Filter status](filter-status.md) — current state of all production filters and in-development versions
- [NexusMind data sources](nexusmind-data-sources.md) — what each production artefact EXCLUDES; `filtered_*.jsonl` also drops source-type-excluded rows, `data/raw/` is pre-enrichment
- [Prefilter & length-floor hypotheses](prefilter-length-floor-hypotheses.md) — what each prefilter actually blocks (measured 2026-08-02); #93 shipped + synced 2026-08-03; read before any enforcement flip
- [Batch-shape score noise](score-batch-shape-noise.md) — **a score is not a function of the article alone**: batch composition alone moves it and flips near-boundary surfacing decisions (#95). ⚠️ **A floor belongs to a population and a mechanism, not to a project** — **five measured terms, and not all are floors**: batch **0.16**, library stack **0.2008**, device CPU→CUDA **0.1956**, oracle decoder **0.436/0.687**, and **host 0.0000** (⛔ **not "cross-box"** — the machine contributes nothing once pins and device match). **Picking one by magnitude looks like rigour and is not a method.** ⛔ **One more is not a floor at all**: `human_thriving v8`'s scope gate is a **step function**, so `1/√k` does not touch it — **5.3% production-mix / 6.7–9.3% at the boundary (n=200)**, and *not distinguishable* from the earlier 13% panel figure. Re-measure per population; never inherit a floor. Read before any threshold or op-point measurement.
- [Enrichment-delta hypotheses](enrichment-delta-hypotheses.md) — **NM#310 is a compute story, not a quality story** (median +0.112, 3.6% of gate crossings, all upward). **H-E1 RESOLVED** — `nature_recovery`'s +0.023 is a **CANCELLATION**, not inertness; the finding that travels is that **enrichment's dominant effect is on evidence-quality-type dimensions** (`cultural_discovery`'s `evidence_quality` **+1.433**, 46/47 rows positive) and `nature_recovery v4` has no evidence dimension. Three open entries: H-E2 (shortest stubs gain LEAST), H-E3 (DeepSeek vs Gemini differ in **slope not offset**), H-E4 (`discovery_novelty`).
- [GPU server](gpu-server.md) — venv, PYTHONPATH, HF_HUB_OFFLINE, ollama conflict, training setup
- [b650 GPU](b650-gpu.md) — 3090 Ti node. **`venv-prodparity` is parity with GPU-SERVER (not sadalsuud); on CPU b650 is bit-identical to it, 660/660.** ⛔ **Production serves on GPU**, so match the DEVICE before diffing any replay against stored production scores — CPU→CUDA is 0.1956, 3 flips at the deployed 4.5. Access, two venvs, staged data
- [Gotcha log](gotcha-log.md) — problem/fix archive
- [Calibration history](calibration-history.md) — Dead Ends section: approaches known to fail (#69); read before calibration/scorer/oracle-prompt work
- [Gemma-3 model](gemma3-model.md) — Auto mapping fix, key format details; read before debugging model loading or PEFT issues
- [Oracle pricing & scheduling](oracle-pricing-scheduling.md) — ⛔ **Gemini Batch is a price we CANNOT PAY** (no `.batches` call site, 2026-08-25): among IMPLEMENTED paths DeepSeek off-peak wins by 1.74×. The answer is a RATIO (I/O < 8.4), and the real lever is the per-prompt cache ceiling, 1.5%–35.7% (#131). Weekends bill off-peak
- [ovr lens set](ovr-lens-set-current.md) — current lens→filter→tab mapping
- [Filter doc standard](filter-doc-standard.md) — deployed filter documentation set
- [CD v5 reference status](cd-v5-reference-status.md) — cultural_discovery v5 as DeepSeek-oracle reference example, ADR-020 methodology
- [Obituary detector](project-obituary-detector.md) — enforcement state, the live false negatives, carryover, and the four SSH verify assertions. Read before touching the junk gates or reading their state.
- [Obituary v4 hypotheses](obituary-v4-hypotheses.md) — small-N hard negatives work, panel beats oracle labels; "FNs are cheap" FALSIFIED 2026-07-30
- [Violence promotion v1 hypotheses](violence-promotion-v1-hypotheses.md) — recipe transfer confirmed, prompt boundary holds, recall gap is the key open question
- [Uplifting v7 training](uplifting-v7-training.md) — training history; v7 deployed (hybrid inference)
- [Thriving v1 scoring](thriving-v1-scoring.md) — PARKED indefinitely (ADR-015); scoring status, resume commands
- **Before 2026-08-01 — 23 session files, no longer indexed line by line** (trimmed 2026-08-25 for the character budget, #123; the files themselves are untouched): `ls memory/project_session_2026_0[57]*.md`. They carry the deployment history of cd v5, nr v4 and solutions v4–v6, the 2026-07-17 deploy hardening, and the 2026-07-31 refit day. ⛔ A pointer is not a summary — open the file, do not infer the session from its name.
