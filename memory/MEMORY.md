# Memory Index

<!-- verify: python3 scripts/verification/check_index_budget.py -->
<!-- verify: python3 scripts/verification/check_index_budget.py --target project -->
<!-- verify: python3 scripts/verification/check_index_budget.py --target pointers -->

⚠️ **This index is budgeted in CHARACTERS, not lines.** It reached **54,808 chars in 91 lines** on 2026-08-15 (up 69% in three days) because entries grow by lengthening, so a line-count heuristic never fires. Session detail belongs in `project_session_*.md`; an entry here is a hook that answers *should I open that file?*

⛔ **THE SESSION LOG HOLDS FOUR ENTRIES. The fifth is MOVED to [`session-log.md`](session-log.md), never compressed.** (#123, 2026-08-25.) The index was 69% session entries by character and grew ~600–900 per session against a fixed ceiling; hand-trimming recovered ~100 chars per line removed, so it lost roughly 6:1 — and every trim turned a finding into a pointer, which is how the always-loaded layer quietly stopped being a record. **Moving costs nothing and loses nothing.** A revision to an existing entry is as expensive as a new one, so budget for both.

*Standing rules moved to `CLAUDE.md` § Hard Constraints → "Working rules" on 2026-08-06 (context audit): they are always-needed constraints, and this file is navigational. This index is pointers + session log only.*


- **2026-08-28 (evening) — three reserved numbers decided, and a probe whose CONTROL outranked it** ([session](project_session_2026_08_28_evening.md)) — **spend $0.12** (90 DeepSeek calls, 0 errors; first oracle spend since 08-23), no model, no threshold, no filter package, **deploy N/A not skipped**. ✅ Owner ruled the three Gate 0 numbers Phase 0 reserved: base rate **19.5% / 2.0× recorded**, stage-1 **held near pass-through**, class-A supplement **3:1 TP:FP** (`docs/decisions/2026-08-28-v8-gate0-corpus-spec.md`). ⛔⛔ **THE KEEPER: the null arm outranked the experiment.** The v8 prompt's cache ceiling was **1.5%, lowest of 17 prompts here**; moving the article to just before §7 (content-preserving to one `---`) lifts it to **95.8%** and the measured rate **0.0% → 90.2%** on the real call site. The reorder then moved 16/30 rows past the #95 band with 3 op-point crossings — **damning until you run the SAME prompt twice: 16/30 and 5 crossings.** The treatment sits inside its own null, so the honest reading is *no effect detectable above noise*, and **without the control the change dies on a number that measures the oracle.** ⭐⭐ What the control exposed: **`scope_verdict` is a binary zeroing all six dimensions, so v8 is a STEP FUNCTION — 13% of identical re-runs flip the gate (median |Δ| 3.750) while gate-stable rows move 0.100**, inside #95's band. `1/√k` cannot touch a Bernoulli; **Gate A missed it by averaging k=3.** A k=1 re-score tosses a coin on ~860 of 6,590 rows, at the boundary → **#135**. **Both findings are one decision: k=3 reordered $10.16 vs k=1 as-is $18.00.** ⛔ Parity **UNPROVEN**; the reordered prompt is written, NOT adopted. ✅ **#134 step 1**: scan set 34 → **202 files**, findings 1 → **339** (338 from `docs/`) — above my pre-registered 60–250, so triaged before reporting: **155 unmarked cross-repo, 170 not-found, 13 collisions**; real decay confirmed (**`docs/README.md` points at three `docs/agents/*.md` that do not exist**), and the FROZEN tier is 2025-11 Qwen-era records that are *correct as history*, so it stays flag-gated. ⛔ Four of mine: **`veen-systems` is this repo's own PARENT and my triage counted it as a sibling** (137 of 156 'ambiguous' against a real 13 — both summed to 156); the **99.4% cache artifact**, **13th occurrence of *establish what a source excludes* on a NEW AXIS — the instrument could not produce a NEGATIVE, so the POSITIVE carried no information**; a sampler reading `raw_weighted_average` at the row root (it **raised**, which is the rule working); and **`CLAUDE.md` cited `ovr.news/BRAND.md` for 15 days — it is `ovr.news/docs/BRAND.md`**, resolved away by our own refcheck's suffix match and caught by `/curate`'s stricter one. **#136** filed (the commit guard cannot read *"Nothing deployed"*).
- **2026-08-28 — v8 Phase 0: the Gate 0 yardstick was measured on a population no draw can sample from** ([repo](../../../repos/veen-systems/llm-distillery/memory/project_session_2026_08_28.md)) — no spend, no model, no filter, nothing deployed, no threshold or probe touched. ⛔⛔ **All five corpus targets were computed over a census that INCLUDES `news.google.com` (22.1%) while the same plan says in bold to exclude it** — base rate 7.74%→**9.76%** (enrichment 3.6×→**2.9×**), p10 length 84→**235**, sub-300-char 30.8%→**11.9%**, pool 206,221 rows→**179,111 articles**. ⭐ *"The short-form regime is under-trained"* is largely a statement about GN headline echoes. **12th occurrence of *establish what a source excludes* — opposite sign: the source INCLUDED what the analysis forbids, and rule and figures sat three paragraphs apart in ONE document.** ⛔ The census scripts imported `hcv1_probe`, absent from disk and from ALL of git history — the published numbers were right, the path to re-derive them had never run; repaired and proven **byte-identical** against the frozen corpus (a frozen input is what makes it a control). ⭐⭐ **Owner caught a real FN trap**: I said FN-rate transfers across prevalence — true for prevalence, **false for MIX**. Production positives are **63.5% marginal**, the corpus's **46.8%** (1.36× toward easy positives), so **v7's probe recall was estimated on an easier population than it serves, today**. Survivable only because the probe **routes 88.6% to stage 2**; a harder screen converts that slack into unrecoverable FNs. ⛔ Two of mine refuted by measurement: predicted row duplication (there is none — 232,845 rows → 232,842 ids) and a spec prescribing density in a band already **4.21×** over-weighted (**134×** at 7.5–8.0) — **a tail is only thin relative to something.** **#134 filed**: refcheck leaves **167 `docs/` files unscanned**; magnitude deliberately not guessed.
- **2026-08-27 — two trims that found three stale claims, and a framework pin that was already current** ([session](project_session_2026_08_27_evening.md)) — no spend, no model, no filter, **nothing deployed**. `CLAUDE.md` **117 → 2,362 bytes free** over two passes, and nothing was cut that lacked a home: seven of the eight fat rows were already homed, checked atom by atom; the one orphan (the GN undercount MECHANISM — `gn_` is **5.1×** low on feeds but **1.73×** on items, so the item-share sanity check reads roughly-right while the feed population is off five-fold) was derived from the file's own table and written into it. ⛔⛔ **THE KEEPER: the audit-time headroom figure is measured at exactly the moment that hides the growth.** I nearly recommended skipping the second trim on "one byte of movement in a full cycle" — that is *headroom at audit time*, and the file is trimmed to the wall then refills: **35,094 → 39,955 in 10 days, ~486/day** over 25 commits. **A quantity sampled only when it is reset cannot show a trend, and it reads as stability.** → **#133** (routing rule vs raise-the-cap vs split; recommendation is the routing rule with a carve-out for money/wrong-number prohibitions). ⭐ **Three stale claims, all in the ALWAYS-LOADED layer, each wrong for weeks**: "109 owned fields" (register regenerated, reads **125**); "the six deployed filters were never assessed" (assessed under #97, closed 08-07, remedy executed 08-06 — verified by reconciling **115 retained + 11 documented = 126** rows still over 500 chars); and `docs/BLOCK_LEDGER_SPEC.md` headed *"NOT yet deployed"* three days after going live. **The pointer table is not just big — it is the part that ROTS**, because it restates numbers that live elsewhere. ✅ **`/update-drift` across six releases: 3 adopt / 1 decline / 4 n/a / 9 in force, stamp v1.26.0 → v1.31.0**, hold discharged with every item named. ⭐ **The globals were ALREADY at v1.31.0** — `audit-context`/`update-drift`/`curate` differ from upstream only by the installer's header rewrite. ⛔ **Diff the install against EVERY tag and read which minimises**; against latest alone you cannot tell *behind by four* from *current plus installer*, and the wrong reading makes an adopter re-copy a re-mapped skill. All 3 adopts (#52, #50, #89) landed in `review-changes` as surgical patches. ⭐ **The controls mattered more than the positives** — the #52 fix sits above the table check, so the run asserted a lossy table STILL fires. ⛔ Three of mine: a verify command that **errored and printed the reassuring branch** I had written myself; `git archive HEAD` as a baseline (**11th** *establish what a source excludes* — 240 fake findings, it ships tracked files only); and a predicted +5 that was never mine. Upstream feedback actioned by the peer session (all three shipped); **agent-ready-projects#100/#101** filed at their request — Step 0's operands collapse with no template dir, and the canonical dead-reference extractor has no cross-repo rung (**12 dead reported, 0 actually dead** here).
- **2026-08-27 — the context layer repaired; a HOLD with no release condition** ([session](project_session_2026_08_27.md)) — no spend, no model, no filter, **nothing deployed** (all 12 files are `CLAUDE.md`/`docs/`/`memory/`/`tests/fixtures/`; deploy is N/A, not skipped). ⛔⛔ **THE KEEPER: a stamp that is HELD and one that is STALE are byte-identical.** The 08-26 triage held `v1.26.0` against upstream `v1.28.0` for a *correct* reason — *"a stamp ahead of its content silences the check that would catch the gap"* — but logged **a COUNT ("3 adopt"), not a CHECKLIST**: one item named, two never written anywhere. So nothing could answer *is the hold dischargeable?*, and **I reported it as neglect**, having not opened the previous session's file before calling something unrecorded. Hold + per-item table now in `docs/decisions/framework-adoption-history.md`; rule: **`/update-drift` names each adopted item and where it lands**. ⭐ **Owner decision open: bump to v1.28.0 or not** — only the owner knows what the two unnamed items were. ✅ **Reference integrity 24 findings → 1**: back-ported #54/#55/#56 + v1.26.1's identifier guard into the FORKED `refcheck.py` (347 vs upstream 796 — patch, never re-copy), plus a local rung-5 extension for auto-memory `project_session_*.md`. Sensitivity **24/24 → 33/33**, nine seeds for what the changes newly PERMIT. **+108 unique refs newly checked (by ABLATION, not a whole-run delta)**; dispositions counted individually: 17 cross-repo qualified, 4 marked, 2 rung-5, **1 left standing on purpose** (a dead `nm188_*` research-script reference in `corroboration-feature-hypotheses.md` — **zero is not the target**). ⭐ **A seeded assertion caught a defect in my own back-port**: `docdir` gated on `isabs()` not *outside ROOT*, so **rung 1b never fired under its own harness** — an assertion on the *rung label* saw it; one on *absence of a finding* would have passed vacuously. ⛔ Mine: a gap reported before reading the record explaining it; **two numbers carried instead of derived** (+107 was a four-part delta attributed to one part; "19 cross-repo" is 17); a `str.replace` anchor missing its closing backtick — `count==1` asserts UNIQUE, never COMPLETE. ⚠️ `CLAUDE.md` 46 → **117 bytes** headroom; the real fix (8 rows = 47% of the table) needs **caveats written into their targets FIRST**. #122 third confirmation + probe: `wc -l` vs what context shows.


- 📓 **Older sessions — [`memory/session-log.md`](session-log.md)** — every entry before the four above, VERBATIM and unabridged (**35** as of 2026-08-28 — `grep -cE '^- ' memory/session-log.md`, do not quote this from memory). The index keeps the newest four; `/curate` MOVES the fifth into the log rather than compressing it, so no finding is ever shortened to buy space. **Look there before concluding something was never recorded.**

- [Date errors & the recency boost](date-error-recency-boost-hypotheses.md) — a flat recency boost on `published_date` means **any** date error landing a row inside the window wins it, invisibly. Both hypotheses RESOLVED. ⚠️ **`collected_date − published_date` is NOT a fabrication instrument without a `source` breakdown** — a fixed-publication-moment source impersonates the signature once per daily cycle. Read before touching dates, the boost (`NexusMind/src/scoring/display_ranking.py`), or any age-derived figure.
- [Hypothesis ledger](hypothesis-ledger.md) — ⭐ **START HERE to recall prior work.** Every hypothesis, its verdict, and where its experiment lives, across 10 files, two repos. ⛔ **`H4` is defined in FOUR files; three files have no ids** — always qualify. Corroboration hypotheses live as `INST/OBS/ART/PROP` rows in NexusMind's V&V registry, the only place recording instrument **certification**.
- [Corroboration feature hypotheses](corroboration-feature-hypotheses.md) — the cosine/time/NER feature set **already exists and was already measured**; what is confirmed, refuted and untested, with the values. **The threshold is not the lever** — the biggest one is a gate, not a feature. Shared-number features refuted; time confirmed and still switched off. Read before proposing any NER or matching-feature work.
- [Google News corpus hypotheses](google-news-corpus-hypotheses.md) — the largest single population in the corpus, and **every number there carries its denominator because four denominator errors were made deriving it**. Five claims already REFUTED. ⚠️ **Never oracle-re-score a GN row**, never match GN on a `gn_` key prefix, and always name whether you mean feeds, rows or items — they differ by up to 5×. Read before quoting **any** GN number.
- [CD v6 probe hypotheses](cd-v6-probe-hypotheses.md) — #98: what the e5 probe confirmed (per-language gap gone; batch-invariant, unlike #95), what it refuted (screening is a REGRESSION; 4 of 5 "recovered" positives are off-lens), and the traps (v6 cannot score — no inference module, no calibration)
- [Cross-repo prioritization](cross-repo-prioritization.md) — master issue landscape across 7 repos. ⛔ **No board count is stated here or at the top of that file — run the `gh` loop at the head of it and quote the result with a timestamp** (removed 2026-08-17, deliberately; do not restore). **The count is never the finding, the distribution is**: a small minority of open issues is touched in any two-day window, ovr.news is the largest board and the least active, and two boards are frozen — all of it re-derivable from that one query. Also holds the four live chains, with the FS#145-is-an-enabler and NM#314-is-built corrections.
- [Stamp & contract integrity](stamp-contract-integrity.md) — **the stamps are a per-article feature set (~40+ numbers), and nothing validated them.** The schemas check SHAPE only and are permissive. **Read before adding a stamp or trusting a stamped field.** ⚠️ **The contract is being REDESIGNED, not patched — read `docs/proposals/contract-a-redesign.md` first; #112 supersedes #111.** **The rule: a field belongs in Contract A iff only the collector can know it AND it is destroyed if not recorded now.** ⭐ **Greenness is not evidence a schema tracks reality.** Also holds the five instrument traps (the validator counts **errors, not rows**), the seven exclusive+perishable categories (**G, the non-event, is the one no symptom surfaces**), and two **superseded** counts kept to date their correction — never re-quote either.
- [Opinion/editorial genre hypotheses](opinion-genre-hypotheses.md) — #121 over 233,338 rows: the within-source control **dissolves the effect in 5 of 6 lenses**; only `investment_risk` survives, and a topic confound survives inside each source. The numbers and the op-point trap are in the `CLAUDE.md` pointer row; the derivations are here. Read before quoting any #121 number or building a genre stamp.
- [Filter status](filter-status.md) — current state of all production filters and in-development versions
- [NexusMind data sources](nexusmind-data-sources.md) — what each production artefact EXCLUDES; `filtered_*.jsonl` also drops source-type-excluded rows, `data/raw/` is pre-enrichment
- [Prefilter & length-floor hypotheses](prefilter-length-floor-hypotheses.md) — what each prefilter actually blocks (measured 2026-08-02); #93 shipped + synced 2026-08-03; read before any enforcement flip
- [Batch-shape score noise](score-batch-shape-noise.md) — **a score is not a function of the article alone**: batch composition alone moves it and flips near-boundary surfacing decisions (#95). ⚠️ **A floor belongs to a population and a mechanism, not to a project** — **five measured terms, and not all are floors**: batch **0.16**, library stack **0.2008**, device CPU→CUDA **0.1956**, oracle decoder **0.436/0.687**, and **host 0.0000** (⛔ **not "cross-box"** — the machine contributes nothing once pins and device match). **Picking one by magnitude looks like rigour and is not a method.** ⛔ **A sixth is not a floor at all**: `human_thriving v8`'s scope gate is a **step function**, so `1/√k` does not touch it — **5.3% production-mix / 6.7–9.3% at the boundary (n=200)**, and *not distinguishable* from the earlier 13% panel figure. Re-measure per population; never inherit a floor. Read before any threshold or op-point measurement.
- [Enrichment-delta hypotheses](enrichment-delta-hypotheses.md) — **NM#310 is a compute story, not a quality story** (median +0.112, 3.6% of gate crossings, all upward). **H-E1 RESOLVED** — `nature_recovery`'s +0.023 is a **CANCELLATION**, not inertness; the finding that travels is that **enrichment's dominant effect is on evidence-quality-type dimensions** (`cultural_discovery`'s `evidence_quality` **+1.433**, 46/47 rows positive) and `nature_recovery v4` has no evidence dimension. Three open entries: H-E2 (shortest stubs gain LEAST), H-E3 (DeepSeek vs Gemini differ in **slope not offset**), H-E4 (`discovery_novelty`).
- [GPU server](gpu-server.md) — venv, PYTHONPATH, HF_HUB_OFFLINE, ollama conflict, training setup
- [b650 GPU](b650-gpu.md) — 3090 Ti node. **`venv-prodparity` is parity with GPU-SERVER (not sadalsuud); on CPU b650 is bit-identical to it, 660/660.** ⛔ **Production serves on GPU**, so match the DEVICE before diffing any replay against stored production scores — CPU→CUDA is 0.1956, 3 flips at the deployed 4.5. Access, two venvs, staged data
- [Gotcha log](gotcha-log.md) — problem/fix archive
- [Calibration history](calibration-history.md) — Dead Ends section: approaches known to fail (#69); read before calibration/scorer/oracle-prompt work
- [Gemma-3 model](gemma3-model.md) — Auto mapping fix, key format details; read before debugging model loading or PEFT issues
- [Oracle pricing & scheduling](oracle-pricing-scheduling.md) — ⛔ **Gemini Batch is a price we CANNOT PAY** (no `.batches` call site, 2026-08-25): among IMPLEMENTED paths DeepSeek off-peak wins by 1.74×. The answer is a RATIO (I/O < 8.4), and the real lever is the per-prompt cache ceiling, 1.5%–35.7% (#131). Weekends bill off-peak
- [ovr lens set](ovr-lens-set-current.md) — current lens→filter→tab mapping
- [Filter doc standard](filter-doc-standard.md) — deployed filter documentation set
- [CD v5 reference status](cd-v5-reference-status.md) — cultural_discovery v5 as DeepSeek-oracle reference example, ADR-020 methodology
- [Obituary detector](project-obituary-detector.md) — enforcement state, the live false negatives, carryover, and the four SSH verify assertions. Read before touching the junk gates or reading their state.
- [Obituary v4 hypotheses](obituary-v4-hypotheses.md) — small-N hard negatives work, panel beats oracle labels; "FNs are cheap" FALSIFIED 2026-07-30
- [Violence promotion v1 hypotheses](violence-promotion-v1-hypotheses.md) — recipe transfer confirmed, prompt boundary holds, recall gap is the key open question
- [Uplifting v7 training](uplifting-v7-training.md) — training history; v7 deployed (hybrid inference)
- [Thriving v1 scoring](thriving-v1-scoring.md) — PARKED indefinitely (ADR-015); scoring status, resume commands
- **Before 2026-08-01 — 23 session files, no longer indexed line by line** (trimmed 2026-08-25 for the character budget, #123; the files themselves are untouched): `ls memory/project_session_2026_0[57]*.md`. They carry the deployment history of cd v5, nr v4 and solutions v4–v6, the 2026-07-17 deploy hardening, and the 2026-07-31 refit day. ⛔ A pointer is not a summary — open the file, do not infer the session from its name.
