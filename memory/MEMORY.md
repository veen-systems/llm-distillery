# Memory Index

<!-- verify: python3 scripts/verification/check_index_budget.py -->
<!-- verify: python3 scripts/verification/check_index_budget.py --target project -->
<!-- verify: python3 scripts/verification/check_index_budget.py --target loaded -->
<!-- verify: python3 scripts/verification/check_index_budget.py --target pointers -->
<!-- verify: python3 scripts/verification/check_doc_claims.py -->
<!-- verify: python3 scripts/verification/check_claim_shapes.py -->

⚠️ **This index is budgeted in SIZE, not lines** (bytes — the guard reads `"rb"`; say bytes when quoting it). It reached **54,808 chars in 91 lines** on 2026-08-15 (up 69% in three days) because entries grow by lengthening, so a line-count heuristic never fires. Session detail belongs in `project_session_*.md`; an entry here is a hook that answers *should I open that file?* ⛔ **This file is NOT auto-loaded** — it is reached by a `CLAUDE.md` pointer row. The always-loaded layer is `CLAUDE.md` plus the USER auto-memory index, budgeted together by `--target loaded` (#138).

⛔ **THE SESSION LOG HOLDS FOUR ENTRIES. The fifth is MOVED to [`session-log.md`](session-log.md), never compressed.** (#123, 2026-08-25.) The index was 69% session entries by character and grew ~600–900 per session against a fixed ceiling; hand-trimming recovered ~100 chars per line removed, so it lost roughly 6:1 — and every trim turned a finding into a pointer, which is how the always-loaded layer quietly stopped being a record. **Moving costs nothing and loses nothing.** A revision to an existing entry is as expensive as a new one, so budget for both.

*Standing rules moved to `CLAUDE.md` § Hard Constraints → "Working rules" on 2026-08-06 (context audit): they are always-needed constraints, and this file is navigational. This index is pointers + session log only.*


- **2026-09-05 (third) — the four owed checks, and the review of them found more than they did** ([session](project_session_2026_09_05_third.md)) — **$0 spend**, no oracle calls, no GPU, **deploy N/A not skipped** (v8 weights gitignored #97, no `human_thriving` in sadalsuud's `NexusMind/filters/`). **EXP-025**, `347a239`/`e6ddb09`, **not pushed**. Built the four claim-shape checks owed by EXP-024's review (`scripts/verification/check_claim_shapes.py`, 43 tests, annotated): **19 real sites over three rounds** on a tree that had just passed the whole battery. ✅ **The phase-8 op-point is UNCHANGED by design weighting** — `phase_c_outcome.py` was unweighted on a **25.1×** design and now prints an HT arm (every prior number reproduces byte-identically, 61 keys added / 0 changed): raw **4.50 → 92.4% junk removed / 56.8% good kept**, junk-removed moving at most **+2.44 pp** and good-kept **+0.20 pp** over all 7 bars and both arms. ⛔ **Those are the two that move LEAST** — specificity, ADR-023's own criterion, moves **+2.65 pp** and recall **−8.51 pp**; base rate **5.3030% → 3.1638%**. ⛔⛔ **THE KEEPER — a four-lens `/review-changes` on a GREEN battery (707 tests, registry, both budget guards, doc-claims, 22/22 annotations) returned 3 blockers and 10 warnings.** (1) The guard written against guards-that-examine-nothing had a scan root matching **0 files** — `".jsonl".endswith((".json",))` is False, so `experiments/registry.jsonl` was unscanned and the `.jsonl` guard beneath it was dead code. (2) **The flagship file survived its own mutation**: `_reads_field` took any non-docstring string constant, so deleting the ONLY real weight read from `phase_c_outcome.py` still PASSED — the name survived in an error message and a JSON label. *Mention is not use*, **4th and 5th occurrences**, the 4th committed by the declaration written to explain the rule. (3) ⭐ **My own fix deleted the TRIGGER, not the defect** — adding a band rewrapped the line so the ordering verb no longer shared it with its two numbers; the site became **invisible**, the check went green, and a published site count failed to reproduce. Re-joined it still FAILED. ⭐ **After any edit made to satisfy a checker, confirm the site is still EXAMINED — count sites before and after, not just the verdict** (22nd occurrence of *establish what it excludes*; the root-that-examined-nothing is the 18th of *prove the outcome changed*). Two of my claims retracted pre-commit: *good-kept at most +0.5 pp* (measured **+0.198**) and *quantified_orderings: 3* (it was **2**). ⭐ A third unbanded ordering measured: `GATING_DECISION.md`'s *"ranks far better"* is **ΔAUC +0.0325, CI [−0.0054,+0.0725], P=0.094 — NOT distinguishable**. ⚙️ **#139's owed look taken**: `import filters.cultural_discovery.v5.inference` succeeds — the shipped filter is fine, the TEST HARNESS is broken; left for the owner. ⭐ **`CLAUDE.md` moved +0 B** while three occurrence entries landed (+3,182 B to `working-rules.md`): `H-CX3`'s monotonic floor is gone, because the 2026-09-04 `rule-ordinals` inversion means the always-loaded copy no longer exists. **`H-CX4` registered** — that mechanising a shape reduces what a lens finds is the thing EXP-025 does NOT claim, and the first evidence points the other way. ✅ **ALL THREE OWED DECISIONS RULED, same session.** (1) **Phase-8 op-point: 4.50 on the CALIBRATED scale** — no code changed, status moved from *inherited* to *re-derived and ratified* (`docs/decisions/2026-09-05-v8-op-point.md`); the argument is the SHAPE of the trade, ~1 good article per junk from 3.75 up, and ADR-023 breaks 1:1 toward specificity. ⚠️ The frontier bends at **3.50** (−3 good buys −11 junk) — go there, not 4.0/4.25, if volume is ever wanted. ⛔ 5.00 was never available (`MAX_NORMALIZATION_RAW_MIN = 4.5`, strict `>`). Verified by EXECUTING `_assign_tier` and by the path proving the comparison is calibrated. (2) **The probes: a PRIVATE Hub repo, not git** — `jeergrvgreg/llm-distillery-probes`, verified by listing it. ⭐ sha256 found **two of the eleven already in git** (`probe_v2.pkl` IS v8's shipped probe) and **four named after production filters that are NOT those filters' probes**. (3) **#139 fixed** — dotted-path import; the shipped filter was never broken, the harness was. **Suite now 711 passed / 0 failed.** ⚙️ **Wrap-up:** v8 **smoke-tested end to end** on b650 (`scripts/gate/v8_smoke_test.py`; a hand-written clear positive scores **4.33 → low**, which is what 4.5 calibrated feels like from the inside) — ⛔ and my first assertion failed for the wrong reason, reading `scorer.calibration` where it lives on `stage2_scorer`. ⛔⛔ **`#97` IS CITED FOR A RULE IT DOES NOT CONTAIN, in ten places, two of them mine today** — it is the TDM assessment and concluded the models are CLEAN; the weights rule is a repo-size rule with **no issue behind it**, and that absence is why everyone reached for the nearest plausible number. Corrected on the live surfaces, left where #97 IS right (~25 sites). ⚠️ My fix cited `.gitignore` by LINE NUMBER and was stale on arrival. ⛔ **The `*_test.*` gitignore trap took a third victim** because the 09-04 rescue was scoped to `docs/evidence/` and said so — *a documented limitation left in place is a defect with a note attached*. **#147 filed** (Stage-1 thresholds inert on 6 of 7 filters; nr v4 config **3.225** vs runtime **0.75**; only v8 pins `probe_sha256`), **#139 closed**, **#104** commented, upstream `agent-ready-projects#127`/`#126` given the adopter data. Next: the phase-8 deploy-gate run.
- **2026-09-05 (second) — the op-point table, and four of its own claims retracted** ([session](project_session_2026_09_05_second.md)) — **$0 spend**, no oracle calls, no GPU, **deploy N/A not skipped** (verified: no `human_thriving` in sadalsuud's `NexusMind/filters/`). **EXP-024**, `293c09d`/`458c209`/`f275094`. Owner asked which scorer architecture to take, wanting **no false positives** — ADR-023's own criterion, **never computed for a probe arm** before this. ✅ **What stands:** at k=17 the two regression probe arms differ by **6 articles, CI [+1,+9]** while their whole-split AUCs are NOT separable — **the op-point test separates them where the ranking metric cannot.** ⛔⛔ **THE KEEPER — FOUR CLAIMS RETRACTED BEFORE COMMIT, AND 667 GREEN TESTS PLUS FOUR GUARDS FOUND NONE OF THEM.** (1) *"AUC would have picked the wrong arm"* — a **coin flip** (Δ**+0.0014**, CI [-0.0448,+0.0476], **P=0.523**) published as an ordering and labelled ⭐⭐ THE REUSABLE FINDING in four places; ⚠️ **the converse kills the generalisation** — AUC separates the student from the probe (**P=0.995**) where the op-point test cannot, so *underpowered* is not excluded. (2) *"the gate buys nothing — identical TP at all eight k"* — **FORCED**: smallest k at which it could differ is **140**, grid stops at **60**. ⭐ *The instrument could not have said no* — **21st occurrence**, one day after the 20th, in a document written about the 20th; ⚠️ and **the conclusion was right**, so nothing pressed anyone to check the evidence cited. (3) A bootstrap that **froze its top-k masks** and emitted a **zero-width 95% CI**; **90.6%** of replicates never surfaced k rows. Now re-selection **with a null control**. (4) **Every figure UNWEIGHTED** on a **25.1×** design-weighted split — weighted, positive rate **3.1638%** not 5.3030% and **the student leads at every share** (0.529 vs 0.478). ⭐ *The headline was a property of the sample.* ⛔ **A fifth found at session close: the arms are on different DEVICES** — student CPU (recall 0.4857 = EXP-015's CPU 0.486 vs CUDA 0.514), probes GPU, against CPU→CUDA 0.1956 (#104); ⭐ conservative **by luck**. Also *"6 of 8 k"*/*"monotonically"* both wrong (8 of 8 / 4 of 8; 6 of 8 was a different arm). ⭐ **A drift report I did NOT file**: `foresight`/`sustainability_technology` still on sadalsuud is **deliberate** (disabled in `enabled_filters`, packages retained to drain ovr's 10-day window) — reading the config first was the whole difference. Rules: *source excludes* → **21st**, *formatter launders exit status* → **3rd** (mine, during `/curate`, seconds after the rule printed on screen). Commented **#104**, **#127**, **#98**. ⚙️ **AFTER curate, the review's own cost went upstream**: **557,442 tokens / 148 tool calls / 4 lenses** this round — commented `agent-ready-projects#126` (already open, the owner's; adds a 2.5× second measurement with **all four lenses completing**, closing its own null, and sizes the overlap: **3 of 4 lenses found the same two shallow findings**, ~250k for a duplicate) ⛔ **but argued against *one lens is enough*** — the zero-blocker lens was the only one that verified the VERIFICATION, so scoring by blockers deletes it. Filed `agent-ready-projects#127` (**no path from a review finding to a mechanical check**, so the cost recurs) and `augmented-engineering#42` (verification-findings evidence). ⚙️ **Four mechanical checks now OWED and in TODO's START HERE**, each to be seeded with a true positive and shown to fire. ▶ **NEXT: phase 8 — unchanged; the op-point is the owner's values call.**
- **2026-09-05 — the device timings get an experiment, and the experiment gets a review** ([session](project_session_2026_09_05.md)) — **$0 spend**, no oracle calls, **deploy N/A not skipped** (verified: no `human_thriving` in sadalsuud's `NexusMind/filters/`). **EXP-022**, then **EXP-023** superseding it. Owner asked whether the CPU/GPU scorer experiments were logged: **the numbers were, the experiment was not** — no cited script, no retained output, host/device/batch only in a docstring, **n=1 per arm**. ⛔⛔ **THE KEEPER — I SEARCHED WHERE THE ARTIFACT COULD NOT BE AND PUBLISHED THE NEGATIVE.** *"The e5-large probe was never retained"* — it was at `b650-gpu:/tmp/probe_e5large.pkl`, with **ELEVEN** probes in that `/tmp` (both EXP-019 regression heads, the seed-42/seed-7 pair), on **36 days of uptime**, one reboot from gone. My `find` was rooted at `/home/jeroen`. ⭐ *The instrument could not have said yes* — inside a document about that exact failure. All rescued, manifest committed, **not in git — owner decision owed**. ⭐⭐ **And the substitution was numerically HARMLESS** (full probe **16.417** vs encoder-only 16.514 — the head is free), *which is why nobody re-checked the premise*. ⛔ **The first re-run reported CUDA as CPU** — 2.37 vs 2.34 ms — because `EmbeddingStage` caches on the **model name alone** (`:112`, read `:214`); true CPU **42.5 ms, 18× slower**. Filed **#146**; latent on the **`(name, device)`** axis, not the name axis (14 configs share e5-small). ⛔ *"The only tell was that the two agreed"* is **false — EXP-019 already recorded 47.2 ms**, a 20× disagreement in the registry this work was repairing. ⭐⭐ **The variance is ARM-SPECIFIC, not a session effect**: three runs of the short GPU arm gave **2.332 / 4.746 / 2.345** (2.04×) while student and CPU arms held to **0.03%** / 0.31% — registered **H-V8-21** with a falsifier. ⛔⛔ **VERIFICATION IS NOT REVIEW, 5th session running** — 667 tests, refcheck, 2 budget guards, 21/21 annotations all green and found **none** of the six defects; `/review-changes` found them all, **four by looking on the machine**. ⭐ A guard refused work I was sure of: rewriting the write-up made **9** EXP-022 metrics untraceable — *append-only protects the ENTRY, and an entry is a pointer*. Corrected figures: student ÷ probe **10.55×**, break-even **56.9%**, saving **1.52%**; **EXP-018/019/020 stand**. Rules: *establish what a source excludes* → **20th**, `pgrep` → **7th**. Commented **#104**, **#81**, **#146**. ▶ **NEXT: phase 8 — unchanged; and decide whether the 11 rescued probes go into git.**
- **2026-09-04 (late) — the cost question answered, and my instrument was wrong three times** ([session](project_session_2026_09_04_late.md)) — **$0 spend**, no oracle calls, nothing in NexusMind or on the Hub, **deploy N/A not skipped** (verified: no `human_thriving` in sadalsuud's `NexusMind/filters/`). **EXP-021**. ⛔⛔ **THE KEEPER — I was one message from sending a peer session a 4× error.** *"~62 ms/article of overhead, a 4× multiplier"* paired sadalsuud's `score` total from a **three-cycle** window (1,209.1 s / 15 calls) with gpu-server's from a **twenty-hour** one (1,656.8 s / 941 batches). Matched: **0.30 ms/article, 1.18×.** ⭐ **The tell was ABSENT — the per-call rates AGREED (80.6 vs 77.5 s/call)**, so every check on the pieces passed and only the denominators were incomparable. ⭐ **"Recompute it a second way" would have CONFIRMED it**; it needs a way that cannot survive the specific confusion. ✅ **The answer**: filter scoring is entirely on gpu-server — 52.1 min per 29.17 h, **1.18×** wall/compute, client+network **0.30 ms/article**. Whole pipeline **5,846.5 s / 5.57% duty cycle**: `score` 53.5%, **story dedup 42.4%**, other stages 4.1%. ⛔ **Dedup is where the box actually works, and it is NOT an embedding cost** — ≤**4.4%** of it is GPU (`/embeddings/encode` 107.8 s against 2,477.5 s); what the other ≥95.6% is was **NOT established**. ⛔⛔ **THREE corrections, all mine, all the same shape, each landing after logging the last**: the two windows; a **config read presented as a runtime proof** (the keys are under `scoring:`, dedup never reads them — and the config predicted the OPPOSITE); and ⭐ **a subtraction named as a category** (*"86% is clustering"* subtracted a **centroid-migration** timer, so the remainder still holds the article pass's blocking wait). Also *"every run logs the GPU device line"* — sent to the peer — is **false, 7 of 8**, and the instrument cannot express "CPU" at all. ⛔⛔ **VERIFICATION IS NOT REVIEW, fourth session running** — 667 tests, refcheck, both budget guards, 21/21 annotations and the structural check all green; `/review-changes` (4 lenses) then found **4 blockers**, incl. all three corrections. ⭐ **The adversarial lens stubbed `journal()` and ran 8 synthetic scenarios through the real `main()`: 8/8 reported a wrong number and exited 0.** ⛔ **Live defect: filtering a journal to a window is not the window being COVERED** — under-coverage reproduces the same error silently (**4.80× against a true 2.40×**); ⭐ *every guard I wrote was a refusal on an EMPTY read, and the defect lives in a PARTIAL one.* Coverage then CHECKED on the real run: both journals span it. Also: **H-V8-16 collision** fixed (capacity → **H-V8-18**), new **H-V8-19** / **H-V8-20**; commented **#141**, **#128**, **#100**, **#104**. ▶ **NEXT: phase 8 — unchanged, and the measurement confirms there is still no Stage-2 cost constraint.**


- 📓 **Older sessions — [`memory/session-log.md`](session-log.md)** — every entry before the four above, VERBATIM and unabridged (**52** as of 2026-09-05 — `grep -cE '^- ' memory/session-log.md`, do not quote this from memory). The index keeps the newest four; `/curate` MOVES the fifth into the log rather than compressing it, so no finding is ever shortened to buy space. **Look there before concluding something was never recorded.**

- [Date errors & the recency boost](date-error-recency-boost-hypotheses.md) — a flat recency boost on `published_date` means **any** date error landing a row inside the window wins it, invisibly. Both hypotheses RESOLVED. ⚠️ **`collected_date − published_date` is NOT a fabrication instrument without a `source` breakdown** — a fixed-publication-moment source impersonates the signature once per daily cycle. Read before touching dates, the boost (`NexusMind/src/scoring/display_ranking.py`), or any age-derived figure.
- [Hypothesis ledger](hypothesis-ledger.md) — ⭐ **START HERE to recall prior work.** Every hypothesis, its verdict, and where its experiment lives, across 10 files, two repos. ⛔ **`H4` is defined in FOUR files; three files have no ids** — always qualify. Corroboration hypotheses live as `INST/OBS/ART/PROP` rows in NexusMind's V&V registry, the only place recording instrument **certification**.
- [Corroboration feature hypotheses](corroboration-feature-hypotheses.md) — the cosine/time/NER feature set **already exists and was already measured**; what is confirmed, refuted and untested, with the values. **The threshold is not the lever** — the biggest one is a gate, not a feature. Shared-number features refuted; time confirmed and still switched off. Read before proposing any NER or matching-feature work.
- [Google News corpus hypotheses](google-news-corpus-hypotheses.md) — the largest single population in the corpus, and **every number there carries its denominator because four denominator errors were made deriving it**. Five claims already REFUTED. ⚠️ **Never oracle-re-score a GN row**, never match GN on a `gn_` key prefix, and always name whether you mean feeds, rows or items — they differ by up to 5×. Read before quoting **any** GN number.
- [CD v6 probe hypotheses](cd-v6-probe-hypotheses.md) — #98: what the e5 probe confirmed (per-language gap gone; batch-invariant, unlike #95), what it refuted (screening is a REGRESSION; 4 of 5 "recovered" positives are off-lens), and the traps (v6 cannot score — no inference module, no calibration)
- [Cross-repo prioritization](cross-repo-prioritization.md) — master issue landscape across 7 repos. ⛔ **No board count is stated here or at the top of that file — run the `gh` loop at the head of it and quote the result with a timestamp** (removed 2026-08-17, deliberately; do not restore). **The count is never the finding, the distribution is**: a small minority of open issues is touched in any two-day window, ovr.news is the largest board and the least active, and two boards are frozen — all of it re-derivable from that one query. Also holds the four live chains, with the FS#145-is-an-enabler and NM#314-is-built corrections.
- [Stamp & contract integrity](stamp-contract-integrity.md) — **the stamps are a per-article feature set (~40+ numbers), and nothing validated them.** The schemas check SHAPE only and are permissive. **Read before adding a stamp or trusting a stamped field.** ⚠️ **The contract is being REDESIGNED, not patched — read `docs/proposals/contract-a-redesign.md` first; #112 supersedes #111.** **The rule: a field belongs in Contract A iff only the collector can know it AND it is destroyed if not recorded now.** ⭐ **Greenness is not evidence a schema tracks reality.** Also holds the five instrument traps (the validator counts **errors, not rows**), the seven exclusive+perishable categories (**G, the non-event, is the one no symptom surfaces**), and two **superseded** counts kept to date their correction — never re-quote either.
- [Opinion/editorial genre hypotheses](opinion-genre-hypotheses.md) — #121 over 233,338 rows: the within-source control **dissolves the effect in 5 of 6 lenses**; only `investment_risk` survives, and a topic confound survives inside each source. The numbers and the op-point trap are in the `CLAUDE.md` pointer row; the derivations are here. Read before quoting any #121 number or building a genre stamp.
- [Filter status](filter-status.md) — current state of all production filters and in-development versions
- [NexusMind data sources](nexusmind-data-sources.md) — what each production artefact EXCLUDES; `filtered_*.jsonl` also drops source-type-excluded rows, `data/raw/` is pre-enrichment
- [Prefilter & length-floor hypotheses](prefilter-length-floor-hypotheses.md) — what each prefilter actually blocks (measured 2026-08-02); #93 shipped + synced 2026-08-03; read before any enforcement flip
- [Batch-shape score noise](score-batch-shape-noise.md) — **a score is not a function of the article alone**: batch composition alone moves it and flips near-boundary surfacing decisions (#95). ⚠️ **A floor belongs to a population and a mechanism, not to a project** — **five measured terms, and not all are floors**: batch **0.16**, library stack **0.2008**, device CPU→CUDA **0.1956**, oracle decoder **0.436/0.687**, and **host 0.0000** (⛔ **not "cross-box"** — the machine contributes nothing once pins and device match). **Picking one by magnitude looks like rigour and is not a method.** ⛔ **One more is not a floor at all**: `human_thriving v8`'s scope gate is a **step function**, so `1/√k` does not touch it — **5.3% production-mix / 6.7–9.3% at the boundary (n=200)**, and *not distinguishable* from the earlier 13% panel figure. Re-measure per population; never inherit a floor. Read before any threshold or op-point measurement.
- [Enrichment-delta hypotheses](enrichment-delta-hypotheses.md) — **NM#310 is a compute story, not a quality story** (median +0.112, 3.6% of gate crossings, all upward). **H-E1 RESOLVED** — `nature_recovery`'s +0.023 is a **CANCELLATION**, not inertness; the finding that travels is that **enrichment's dominant effect is on evidence-quality-type dimensions** (`cultural_discovery`'s `evidence_quality` **+1.433**, 46/47 rows positive) and `nature_recovery v4` has no evidence dimension. Three open entries: H-E2 (shortest stubs gain LEAST), H-E3 (DeepSeek vs Gemini differ in **slope not offset**), H-E4 (`discovery_novelty`).
- [GPU server](gpu-server.md) — venv, PYTHONPATH, HF_HUB_OFFLINE, ollama conflict, training setup
- [b650 GPU](b650-gpu.md) — 3090 Ti node. **`venv-prodparity` is parity with GPU-SERVER (not sadalsuud); on CPU b650 is bit-identical to it, 660/660.** ⛔ **Production serves on GPU**, so match the DEVICE before diffing any replay against stored production scores — CPU→CUDA is 0.1956, 3 flips at the deployed 4.5. Access, two venvs, staged data
- [Gotcha log](gotcha-log.md) — problem/fix archive
- [Calibration history](calibration-history.md) — Dead Ends section: approaches known to fail (#69); read before calibration/scorer/oracle-prompt work
- [Gemma-3 model](gemma3-model.md) — Auto mapping fix, key format details; read before debugging model loading or PEFT issues
- [Oracle pricing & scheduling](oracle-pricing-scheduling.md) — ⛔ **Gemini Batch is a price we CANNOT PAY** (no `.batches` call site, 2026-08-25): among IMPLEMENTED paths DeepSeek off-peak wins by 1.74×. The answer is a RATIO (I/O < 8.4), and the real lever is the per-prompt cache ceiling, 1.5%–35.7% (#131). Weekends bill off-peak
- [ovr lens set](ovr-lens-set-current.md) — current lens→filter→tab mapping
- [Filter doc standard](filter-doc-standard.md) — deployed filter documentation set
- [CD v5 reference status](cd-v5-reference-status.md) — cultural_discovery v5 as DeepSeek-oracle reference example, ADR-020 methodology
- [Obituary detector](project-obituary-detector.md) — enforcement state, the live false negatives, carryover, and the four SSH verify assertions. Read before touching the junk gates or reading their state.
- [Obituary v4 hypotheses](obituary-v4-hypotheses.md) — small-N hard negatives work, panel beats oracle labels; "FNs are cheap" FALSIFIED 2026-07-30
- [Violence promotion v1 hypotheses](violence-promotion-v1-hypotheses.md) — recipe transfer confirmed, prompt boundary holds, recall gap is the key open question
- [Uplifting v7 training](uplifting-v7-training.md) — training history; v7 deployed (hybrid inference)
- [Thriving v1 scoring](thriving-v1-scoring.md) — PARKED indefinitely (ADR-015); scoring status, resume commands
- **Before 2026-08-01 — 23 session files, no longer indexed line by line** (trimmed 2026-08-25 for the character budget, #123; the files themselves are untouched): `ls memory/project_session_2026_0[57]*.md`. They carry the deployment history of cd v5, nr v4 and solutions v4–v6, the 2026-07-17 deploy hardening, and the 2026-07-31 refit day. ⛔ A pointer is not a summary — open the file, do not infer the session from its name.
