# adj3 band audit RE-RUN — NOT USED: the drift check failed again (2026-09-25)

**Pass A vs pass B: 0.887 on 80 rows (71 agree; 72 needed for 0.90).** Per PREREGISTRATION.md the run
is not used; the junk-per-cut-off table was not computed (`analyse.py` stops first; `result.txt`).
Controls 6/6, including the two new ones testing rulings 4 and 5, so the rulings were applied.

Owner check: **15/20** blind, on translated full text this time. Not resolved: the run is not used.
Disagreements: #8 teacher contest with a cash prize (owner IN), #10 longevity lifestyle tips (IN),
#13 nurse-navigator pain pilot (IN; judge A/B split → tie-out), #19 student cheap-recipes story (IN),
#20 navy rescues 107 migrants (owner OUT, judge IN).

## What two failed runs say
Below adj3's 4.5 op-point, Thriving scope is close to a matter of taste. The same judge disagrees with
itself on ~11%, the owner disagrees with it on ~25%, and each ruling closes a category only for the
next sample to surface new ones (run 1: sport, paywalled teasers; run 2: lifestyle advice, contest
prizes, rescues, pilots). This instrument, at this bar, cannot measure junk in this band.
It measures the band as **ambiguous**, which is not the same as **junk**.

## Process notes
- Excerpt error, again the assistant's: #6 (Polish pension) was first shown cut before the amount;
  the owner called IN, saw the full text, and re-called OUT before any comparison. Both are recorded.
- Judge isolation (own `/tmp/judge_*` dirs) worked: no cross-writes.
